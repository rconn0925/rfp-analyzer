---
phase: 01-parsing-structure-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/corpus/MANIFEST.md
  - tests/corpus/manifest.json
autonomous: false
requirements: [PARS-01, PARS-02]
user_setup:
  - service: SAM.gov
    why: "API key needed for Phase 5 SAM.gov fetch; ~10 business day issuance — request must start now (Ross-personal action, cannot be automated)"
    env_vars:
      - name: SAM_API_KEY
        source: "SAM.gov -> sign in via login.gov -> Account Details page -> request/view API key (not used until Phase 5; store outside the repo)"

must_haves:
  truths:
    - "Three real SAM.gov packages exist locally under tests/corpus/ per D-01: one clean full-UCF multi-file solicitation with an SF30 amendment, one hostile package with scanned pages, one FAR Part 12 combined synopsis/solicitation"
    - "git status shows zero corpus binaries as untracked-and-addable (all ignored); only MANIFEST.md and manifest.json are tracked"
    - "MANIFEST.md documents every package: SAM.gov notice link, solicitation number, file list with sha256, page counts, selection rationale"
    - "The SAM.gov API key request has been surfaced to Ross as a personal action item"
  artifacts:
    - path: "tests/corpus/MANIFEST.md"
      provides: "Human-readable corpus documentation per D-02"
      contains: "sha256"
    - path: "tests/corpus/manifest.json"
      provides: "Machine-readable package metadata (dir, role, expected properties) consumed by plan 01-06 integration tests"
      contains: "\"role\""
  key_links:
    - from: "tests/corpus/manifest.json"
      to: "tests/corpus/<package-dirs>"
      via: "dir field naming each package directory"
      pattern: "\"dir\""
---

<objective>
Acquire the 3-package real-world test corpus from SAM.gov (D-01, manual browser download — no API key needed), document it in a committed MANIFEST.md plus a machine-readable manifest.json (D-02), keep all binaries out of git, and surface the SAM.gov API key request as Ross's personal action item.

Purpose: Every heuristic in plans 01-04/01-05 is corpus-tuned, and plan 01-06's integration tests and human verification run against these exact packages. The primary package becomes Phase 2's hand-shredded golden set — it must be chosen deliberately.
Output: tests/corpus/ populated locally with 3 packages; MANIFEST.md + manifest.json committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-parsing-structure-foundation/01-CONTEXT.md
@.planning/phases/01-parsing-structure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Download the 3-package SAM.gov corpus and start the API key request (Ross-personal)</name>
  <action>Human-only step: browser session on sam.gov to download the three D-01 packages into tests/corpus/ subdirectories, plus the login.gov/SAM.gov API key request. No automation is possible (no API key exists yet; downloads are browser-driven). Full instructions in how-to-verify below.</action>
  <what-built>Automated everything possible: .gitignore already excludes tests/corpus/* (plan 01-01), and Task 2 below automates checksumming, page counting, and manifest generation. The downloads themselves require a human browser session on sam.gov — there is no API access yet (and the key takes ~10 business days), so this step cannot be automated.</what-built>
  <how-to-verify>
    Two personal actions for Ross:

    A) START THE SAM.GOV API KEY REQUEST NOW (Phase 5 dependency, ~10 business day issuance):
       1. Sign in at sam.gov via login.gov (create the login.gov account if needed)
       2. Go to your SAM.gov Account Details page and request/generate the Public API key
       3. Store it outside the repo (password manager / env var); it is NOT used until Phase 5

    B) DOWNLOAD THE 3-PACKAGE CORPUS (sam.gov → Contract Opportunities search; no account needed to download attachments). Create one subdirectory per package under tests/corpus/ (short slug names, e.g. tests/corpus/primary-ucf/, tests/corpus/hostile-scanned/, tests/corpus/non-ucf-part12/) and save all package files into it:

       1. PRIMARY (clean full-UCF): filter Notice Type = "Solicitation"; DoD services solicitations (Army/Navy/Air Force, NAICS 5415xx or 561xxx) are most reliably full-UCF with SF33 covers. Requirements: base solicitation PDF + at least ONE SF30 amendment + attachments; ~100-300 pages total; open the base PDF and confirm you can see "SECTION L" and "SECTION M" headings and select/copy text. Choose deliberately — this same package becomes Phase 2's hand-shredded golden set.
       2. HOSTILE (scanned pages): ideal specimen is a signed-and-rescanned SF30 amendment (open the PDF, Ctrl+A copies nothing = no text layer). Older or smaller-agency notices are the best hunting ground. If the primary package's amendment is born-digital, source the scanned SF30 from a different notice — the hostile package may be any package that contains scanned pages.
       3. NON-UCF: filter Notice Type = "Combined Synopsis/Solicitation" — these are FAR 12.603 packages (the exact honest-degradation specimen). An SF1449-based Part 12 solicitation is an acceptable substitute.

       For each package, note the SAM.gov notice URL and solicitation number (paste them in your reply or drop a notes.txt in each package dir — Task 2 needs them for the MANIFEST).
  </how-to-verify>
  <resume-signal>Reply "corpus ready" with the three notice URLs + solicitation numbers (or confirm notes.txt files exist), and confirm the API key request was submitted (or state you're deferring it)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generate MANIFEST.md + manifest.json with checksums, page counts, and package roles; commit</name>
  <files>tests/corpus/MANIFEST.md, tests/corpus/manifest.json</files>
  <read_first>
    - .gitignore (confirm tests/corpus/* exclusion with !MANIFEST.md and !manifest.json exceptions before touching corpus)
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md ("Corpus Sourcing" section and D-02 in User Constraints — required MANIFEST fields)
    - tests/corpus/ directory listing (the packages Ross downloaded, plus any notes.txt)
  </read_first>
  <action>
    1. Verify hygiene first: `git status --porcelain tests/corpus/` must show nothing except MANIFEST.md/manifest.json — if any binary shows as untracked-addable, STOP and fix .gitignore before proceeding.
    2. Write a throwaway script (scratchpad, not committed) that walks tests/corpus/*/ and for each file computes sha256 (hashlib streaming, 1MB chunks per RESEARCH "Stable file identity"), file size, and page count for PDFs (pdfplumber `len(pdf.pages)`; wrap per-file in try/except — a hostile corpus file that crashes pdfplumber gets page_count "unreadable", which is itself corpus intel worth recording).
    3. Write tests/corpus/MANIFEST.md per D-02, one section per package: SAM.gov notice URL, solicitation number, role (primary-golden-set / hostile-scanned / non-ucf-part12), selection rationale (why THIS package — e.g. "SF33 cover, 2 SF30 amendments, PWS attachment, 240 pages total"), and a file table: filename, sha256, size, page count.
    4. Write tests/corpus/manifest.json — machine-readable (stdlib json, no new deps), consumed by plan 01-06 integration tests. Top-level key "packages": list of objects with fields: "dir" (subdirectory name), "role" (one of "primary", "hostile", "non_ucf"), "expected_classification" ("full_ucf" for primary; "non_ucf_commercial" for the non-UCF package; hostile's honest expected value based on what the package is), "has_amendment" (true for primary per D-01), "has_scanned_pages" (true for hostile), "solicitation_number", "notice_url". If Ross supplied notes.txt files, fold their content in and delete them (they live in gitignored space anyway).
    5. Commit ONLY the two manifest files: `git add tests/corpus/MANIFEST.md tests/corpus/manifest.json`, conventional commit (e.g. `docs(01-02): document 3-package SAM.gov test corpus`), push, confirm `git ls-files tests/corpus/` lists exactly those two files.
  </action>
  <verify>
    <automated>uv run python -c "import json; d=json.load(open('tests/corpus/manifest.json')); roles=sorted(p['role'] for p in d['packages']); assert roles==['hostile','non_ucf','primary'], roles; assert any(p.get('has_amendment') for p in d['packages']); assert any(p.get('has_scanned_pages') for p in d['packages'])"</automated>
  </verify>
  <done>Both manifests committed and pushed; corpus binaries local-only; all three roles documented.</done>
  <acceptance_criteria>
    - `git ls-files tests/corpus/` outputs exactly MANIFEST.md and manifest.json
    - `ls tests/corpus/` shows 3 package subdirectories (plus the two manifests)
    - MANIFEST.md contains 3 sha256-bearing file tables and 3 sam.gov URLs (`grep -c "sam.gov" tests/corpus/MANIFEST.md` >= 3)
    - manifest.json parses with stdlib json and contains roles primary, hostile, non_ucf, one `"has_amendment": true`, one `"has_scanned_pages": true`
    - `git status --porcelain tests/corpus/` is empty after commit (nothing untracked-addable)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SAM.gov downloads → local filesystem | Real-world files of unknown internal structure land in the repo working tree |
| local corpus → public GitHub | Accidental commit would push large binaries (public data, but repo hygiene failure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | git commit of corpus binaries | mitigate | Task 2 step 1 hygiene gate: `git status --porcelain tests/corpus/` must be clean before proceeding; explicit `git add` of only the two manifest files (never `git add -A` in this plan) |
| T-01-04 | Denial of Service | pdfplumber page-count pass over hostile files | mitigate | Per-file try/except in the manifest script; unreadable files recorded as "unreadable", run continues (mirrors RESEARCH Pitfall 2) |
| T-01-05 | Tampering | Corpus files treated as trusted later | accept | Files are public SAM.gov data hashed at intake (sha256 in MANIFEST); parsing plans treat them as hostile input regardless — no trust is extended here |
| T-01-SC | Tampering | package installs | accept | No new packages installed in this plan (manifest generation and verification use stdlib json/hashlib + already-audited pdfplumber) |
</threat_model>

<verification>
- 3 package directories exist locally under tests/corpus/ with the D-01 composition
- Only MANIFEST.md + manifest.json tracked; CI stays green (no corpus needed by unit tests)
- Ross confirmed the SAM.gov API key request status
</verification>

<success_criteria>
- Corpus matches D-01 exactly: clean full-UCF w/ SF30 + hostile scanned + FAR Part 12 combined synopsis
- D-02 satisfied: binaries gitignored, MANIFEST.md committed with links/numbers/checksums/rationale
- manifest.json gives plan 01-06 deterministic per-package expectations
</success_criteria>

<output>
Create `.planning/phases/01-parsing-structure-foundation/01-02-SUMMARY.md` when done
</output>
