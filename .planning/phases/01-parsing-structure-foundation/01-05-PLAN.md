---
phase: 01-parsing-structure-foundation
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - src/rfp_analyzer/pipeline/sectioning/__init__.py
  - src/rfp_analyzer/pipeline/sectioning/headings.py
  - src/rfp_analyzer/pipeline/sectioning/tree.py
  - src/rfp_analyzer/pipeline/classify/__init__.py
  - src/rfp_analyzer/pipeline/classify/forms.py
  - src/rfp_analyzer/pipeline/classify/package.py
  - tests/unit/test_headings.py
  - tests/unit/test_tree.py
  - tests/unit/test_forms.py
  - tests/unit/test_package_classify.py
autonomous: true
requirements: [PARS-02]

must_haves:
  truths:
    - "A standard UCF solicitation yields SectionNodes for L, M, C (and H when present) with page-range locators that do NOT all point at page 1 (TOC pages are recognized, never treated as section starts)"
    - "SF30 amendment files are labeled doc_role 'amendment' — via form text when a text layer exists, via filename fallback (marked unverified) when scanned"
    - "A FAR Part 12 combined synopsis (or SF1449 package) classifies as non_ucf_commercial with human-readable evidence and warnings — never a fabricated empty UCF section tree"
    - "An attachment titled 'Proposal Submission Instructions' with no 'SECTION L' literal still gets the instructions role via role-title matching"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/sectioning/headings.py"
      provides: "SECTION_HEADING/PART_HEADING/PARA_NUMBER regexes, role-title table, text normalization, per-page candidate detection"
      exports: ["find_heading_candidates", "normalize_line"]
    - path: "src/rfp_analyzer/pipeline/sectioning/tree.py"
      provides: "TOC disambiguation + build_section_tree with PageSpan/BlockSpan locators"
      exports: ["build_section_tree"]
    - path: "src/rfp_analyzer/pipeline/classify/forms.py"
      provides: "SF33/SF1449/SF30/combined-synopsis signature detection + SF30 ladder + amendment number extraction"
      exports: ["detect_form", "assign_doc_role"]
    - path: "src/rfp_analyzer/pipeline/classify/package.py"
      provides: "classify_package aggregating trees + form signals into full_ucf/partial_ucf/non_ucf_commercial/unknown with evidence + warnings"
      exports: ["classify_package"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/sectioning/tree.py"
      to: "src/rfp_analyzer/pipeline/models.py"
      via: "emits SectionNode with Locator union"
      pattern: "SectionNode"
    - from: "src/rfp_analyzer/pipeline/classify/package.py"
      to: "src/rfp_analyzer/pipeline/models.py"
      via: "returns DocumentMap classification literal + evidence/warnings lists"
      pattern: "non_ucf_commercial"
---

<objective>
Implement PARS-02's core: UCF section-boundary detection (heading regexes + role titles + form anchors, with TOC disambiguation), the section tree builder over cleaned pages/blocks, SF form-page recognition (SF33/SF1449/SF30 + FAR 12.603 combined-synopsis markers), the SF30 amendment detection ladder, and honest package classification (full_ucf / partial_ucf / non_ucf_commercial / unknown, always with evidence and warnings).

Purpose: Success criteria 2 and 4 — correct UCF boundaries on standard solicitations, honest degradation on non-UCF packages, and labeled SF30 amendments. These heuristics are THE novel code of Phase 1 (RESEARCH "Key insight").
Output: `sectioning` and `classify` pipeline packages, fully unit-tested on synthetic text (corpus validation happens in plan 01-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-parsing-structure-foundation/01-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/quality/headers.py

<interfaces>
Inputs to this plan's functions (read actual files for exact shapes):
- ParsedFile after apply_quality (plan 01-04): PDF pages have final quality + cleaned text ("" for non-ok pages); DOCX files have BlockInfo blocks with style names; first_page_layout_text holds layout-mode page-1 text for form-field extraction
- SectionNode(label, title, role, locator: PageSpan|BlockSpan, detection: "form_anchor"|"heading"|"role_title"|"paragraph_numbering", children)
- ParsedFile.doc_role / amendment_number / amendment_evidence are THIS plan's outputs per file; DocumentMap.classification/classification_evidence/warnings are computed by classify_package
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Heading regexes, role-title table, and per-page candidate detection</name>
  <files>src/rfp_analyzer/pipeline/sectioning/__init__.py, src/rfp_analyzer/pipeline/sectioning/headings.py, tests/unit/test_headings.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 3 — the three signal classes, regex sketches SECTION_HEADING/PART_HEADING/PARA_NUMBER, normalization recipe, role-title strings with FAR 15.204-1 citation; Assumption A5)
    - src/rfp_analyzer/pipeline/models.py (SectionNode.role literal values, detection literal values)
  </read_first>
  <behavior>
    - Test 1: "SECTION L - INSTRUCTIONS, CONDITIONS, AND NOTICES TO OFFERORS" and "SECTION L—INSTRUCTIONS..." and "section l: instructions" all match with letter "L" (case/dash/colon tolerant); "SECTION L" mid-sentence ("see SECTION L for details") does NOT match (line-start anchored)
    - Test 2: "PART I—THE SCHEDULE" through "PART IV" match PART_HEADING; "L.4.2" at line start matches PARA_NUMBER with letter L and components (4,2); "1.4.2" does not
    - Test 3: role-title matching maps "INSTRUCTIONS, CONDITIONS, AND NOTICES TO OFFERORS" → role "instructions"; "EVALUATION FACTORS FOR AWARD" → "evaluation"; "STATEMENT OF WORK" and "PERFORMANCE WORK STATEMENT" → "sow_pws"; "SPECIAL CONTRACT REQUIREMENTS" → "special_requirements" — matching survives normalization (mixed case, wrapped whitespace)
    - Test 4: find_heading_candidates over a page's text returns candidates with line index, matched letter/label, title text, and signal type ("heading" | "role_title" | "paragraph_numbering") — a page containing both "SECTION L" and body "L.1 General" yields both candidate types
    - Test 5: regexes complete instantly on adversarial input (e.g. "SECTION " + "L"*50000 and 10k lines of near-matches) — test asserts sub-second wall time (linear patterns, no catastrophic backtracking)
  </behavior>
  <action>
    Create `headings.py` with: `normalize_line(s: str) -> str` (collapse whitespace runs, unify en/em dashes and hyphens to "-", uppercase, strip) per Pattern 3's normalization note; module-level compiled regexes SECTION_HEADING, PART_HEADING, PARA_NUMBER following the RESEARCH Pattern 3 sketches (line-start anchored via MULTILINE, case-insensitive, dash/colon/period tolerant, bounded quantifiers — keep the `.{0,120}` / `.{0,80}` caps exactly to prevent unbounded scans); a ROLE_TITLES mapping of normalized title substrings → SectionNode.role values covering the four Pattern 3 role strings plus "PROPOSAL SUBMISSION INSTRUCTIONS" → "instructions" (the Pitfall-3-class attachment case); and `find_heading_candidates(page_text: str) -> list[HeadingCandidate]` (small dataclass: line_index, label, title, role, signal) that scans normalized lines and returns every match with its signal class. Pure functions, no file I/O. Tag A5 assumption in docstrings — these exact forms are corpus-falsifiable by design.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_headings.py -x -q</automated>
  </verify>
  <done>All heading/role/paragraph signals detected per behaviors; adversarial-input timing test passes.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.sectioning.headings import find_heading_candidates, normalize_line, SECTION_HEADING"` exits 0
    - Mid-sentence "SECTION L" non-match asserted (anchoring proven)
    - "PROPOSAL SUBMISSION INSTRUCTIONS" → "instructions" asserted (role-title catches attachment-style files)
    - Timing test asserts < 1.0s on the adversarial inputs
    - pytest exit 0, >= 5 tests
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TOC disambiguation and section tree builder (PDF page spans + DOCX block spans)</name>
  <files>src/rfp_analyzer/pipeline/sectioning/tree.py, tests/unit/test_tree.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 3 "TOC disambiguation (critical)" — many-headings-page rule, trailing page numbers/dot leaders, prefer-later-leading-line heuristic, Assumption A7; Pitfall 1; Pitfall 4 — DOCX style-first + text fallback; anti-pattern "Treating TOC lines as section starts")
    - src/rfp_analyzer/pipeline/sectioning/headings.py (Task 1 — candidates consumed here)
    - src/rfp_analyzer/pipeline/models.py (SectionNode, PageSpan, BlockSpan)
  </read_first>
  <behavior>
    - Test 1 (Pitfall 1, the critical one): a synthetic file whose page 1 lists ALL section titles "SECTION A ... SECTION M" (TOC, lines with trailing page numbers and dot leaders) and whose real "SECTION L" heading opens page 40 with body text → the L node's locator is PageSpan(page_start=40, ...), NOT page 1; page 1 is recorded as TOC (not a section start)
    - Test 2: a page containing >= TOC_MIN_DISTINCT_HEADINGS (default 4) distinct section-letter headings is treated as a TOC page even without dot leaders
    - Test 3: sections nest — L heading on page 40, "L.1" and "L.4" paragraph numbers later → children of L with correct sub-spans; L's page_end is the page before the next top-level section start (M), last section runs to file end
    - Test 4: DOCX tree — blocks with style "Heading 1" text "SECTION L ..." at ordinal 10 and "SECTION M ..." at ordinal 50 → SectionNodes with BlockSpan(10, 49) and BlockSpan(50, last); a DOCX with NO heading styles but ALL-CAPS "SECTION L" paragraph text still produces the L node via text heuristics (Pitfall 4 fallback)
    - Test 5: a file with zero heading candidates returns an empty section list (never a fabricated node), and scanned pages (text == "") contribute no candidates
  </behavior>
  <action>
    Create `tree.py` exporting `build_section_tree(file: ParsedFile) -> list[SectionNode]`. PDF path: collect find_heading_candidates per ok page; mark TOC pages — a page is a TOC when it has >= TOC_MIN_DISTINCT_HEADINGS=4 distinct top-level section-letter matches (named constant) OR a majority of its heading lines end in a page-number token / dot-leader pattern (regex like `\.{3,}\s*\d+\s*$` or trailing standalone integer); implement the A7 start-page rule: a section's start = the page where its heading appears as a leading line on a non-TOC page, preferring the LAST such occurrence that still precedes at least one subsequent distinct section heading (guards against both the page-1 TOC and mid-document cross-references); top-level nodes are section letters (A–M order when detected) with role-title nodes (SOW/PWS attachments) as top-level role-tagged nodes when no letter applies; children from PARA_NUMBER matches within the parent's span (L.1, L.4 → depth by dotted components); page_end = page before next sibling's start, else last page; detection field set per originating signal ("heading" | "role_title" | "paragraph_numbering"). DOCX path: candidate = block where style.name startswith "Heading" AND/OR text matches the same heading/role regexes (style-first, text-heuristic fallback per Pitfall 4); locators are BlockSpan ordinals with the same next-sibling end rule. Never emit a node without a matched signal; empty list is the honest empty answer. Keep TOC page indices as a returned-or-attached artifact (e.g. second return value or SectionNode-free record) so classify/report can mention "TOC on page 1".
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_tree.py -x -q</automated>
  </verify>
  <done>TOC pages never seed sections; page/block spans correct per behaviors; DOCX fallback works without styles.</done>
  <acceptance_criteria>
    - Test 1 asserts L.locator.page_start == 40 with the TOC on page 1 (the exact Pitfall 1 regression)
    - `uv run python -c "from rfp_analyzer.pipeline.sectioning.tree import build_section_tree, TOC_MIN_DISTINCT_HEADINGS"` exits 0
    - DOCX no-styles fallback test passes (node produced from text heuristics alone)
    - No test asserts a section for input with zero candidates (honest-empty proven by Test 5)
    - pytest exit 0, >= 5 tests
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Form signatures, SF30 amendment ladder, and package classification</name>
  <files>src/rfp_analyzer/pipeline/classify/__init__.py, src/rfp_analyzer/pipeline/classify/forms.py, src/rfp_analyzer/pipeline/classify/package.py, tests/unit/test_forms.py, tests/unit/test_package_classify.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 4 — verified form title table incl. BOTH SF1449 title variants and the no-form FAR 12.603 markers; SF30 detection ladder; Pitfall 5; Pattern 6 "classification rules" design note; Open Question 1 — SF1449 + detected L/M ⇒ partial_ucf, not non_ucf; Open Question 3 — amendment number near Block 2 label)
    - src/rfp_analyzer/pipeline/sectioning/tree.py (Task 2 — trees consumed by classify_package)
    - src/rfp_analyzer/pipeline/models.py (doc_role, amendment_evidence, classification literals)
  </read_first>
  <behavior>
    - Test 1: page-1 text containing "SOLICITATION, OFFER AND AWARD" → form "sf33"; "SOLICITATION/CONTRACT/ORDER FOR COMMERCIAL PRODUCTS AND COMMERCIAL SERVICES" AND the older "...FOR COMMERCIAL ITEMS" variant → "sf1449"; "AMENDMENT OF SOLICITATION/MODIFICATION OF CONTRACT" → "sf30"; matching is case/whitespace/slash tolerant against whitespace-collapsed text
    - Test 2: SF30 ladder rung 1 — file with SF30 title text → doc_role "amendment", amendment_evidence "form_text", amendment_number extracted from text near "AMENDMENT/MODIFICATION NO" label (accepts None when not found, still labeled amendment)
    - Test 3: SF30 ladder rung 2 — file whose page 1 is scanned (empty text) but filename "Amendment_0001_signed.pdf" (also test "sf30.pdf", "amdt2.pdf", "P00001.pdf"-style mod numbers) → doc_role "amendment", amendment_evidence "filename"; a scanned file named "attachment_J1.pdf" → NOT an amendment (rung 3)
    - Test 4: classify_package — files yielding L+M+C/sow trees → "full_ucf"; L+M but no C/SOW → "partial_ucf" with warning naming the missing section; SF1449 form + detected L/M sections → "partial_ucf" (Open Question 1 rule); SF1449 or combined-synopsis markers with no UCF sections → "non_ucf_commercial" with evidence naming the trigger; nothing detected → "unknown"
    - Test 5: every non-full_ucf classification carries >= 1 human-readable warning and evidence list is never empty for any classification (honest degradation is articulate, not just an enum)
  </behavior>
  <action>
    Create `forms.py` with: FORM_SIGNATURES mapping — normalized (whitespace-collapsed, uppercased) title substrings per Pattern 4's verified table: SF33 "SOLICITATION, OFFER AND AWARD"; SF1449 both variants; SF30 "AMENDMENT OF SOLICITATION/MODIFICATION OF CONTRACT"; COMBINED_SYNOPSIS_MARKERS = ["COMBINED SYNOPSIS/SOLICITATION", "SUBPART 12.6", "FAR 12.603"] (FAR 12.603 packages use NO form — text markers are the only signal); `detect_form(file: ParsedFile) -> FormMatch | None` matching against whitespace-collapsed page-1 text, falling back to first_page_layout_text when default-mode text misses (form layouts split titles across words — RESEARCH Pattern 4 note); AMENDMENT_FILENAME_RE covering tokens sf30, amend, amdt, mod, a####/p####/#### suffix styles; `assign_doc_role(file) -> ParsedFile` implementing the Pattern 4 ladder exactly: (1) SF30 title text → amendment + evidence "form_text" + amendment_number regex near the "AMENDMENT/MODIFICATION NO" label (try default text then first_page_layout_text; accept None); (2) no page-1 text layer AND filename matches → amendment + evidence "filename" (report layer in 01-06 renders this as "unverified — scanned, matched by filename"); (3) neither → base_solicitation when SF33/SF1449 matched, else attachment. Never let a scanned SF30 silently become a plain attachment (Pitfall 5 — the test corpus's hostile specimen exists to catch this).
    Create `package.py` with `classify_package(files: list[ParsedFile]) -> tuple[classification, evidence, warnings]` implementing Pattern 6 rules: full_ucf when L, M, and (C or a sow_pws-role node) all detected across the package with sane ordering; partial_ucf when some-but-not-all (warning names each missing section, e.g. "No Section M heading found; downstream results may be incomplete"); non_ucf_commercial on SF1449/combined-synopsis signals WITHOUT detected UCF sections — but SF1449 WITH detected L/M → partial_ucf (form and structure signals are independent, Open Question 1); unknown otherwise; evidence strings always populated (e.g. "SF33 title matched on base.pdf page 1", "Section L found at pages 40-52 of base.pdf").
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_forms.py tests/unit/test_package_classify.py -x -q && uv run pytest tests/unit -q</automated>
  </verify>
  <done>Form detection, SF30 ladder, and all four classification outcomes tested; full unit suite green.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.classify.forms import detect_form, assign_doc_role; from rfp_analyzer.pipeline.classify.package import classify_package"` exits 0
    - Both SF1449 title variants asserted in test_forms.py (grep "COMMERCIAL ITEMS" and "COMMERCIAL PRODUCTS" each >= 1 in the test file)
    - Scanned-SF30 filename-fallback test asserts amendment_evidence == "filename" (Pitfall 5 regression)
    - All four classification literals asserted at least once in test_package_classify.py
    - pytest full unit suite exit 0; commit + push, CI green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleaned document text → regex heuristics | Hostile text (adversarial lengths, near-match floods) processed by pattern matching |
| filenames → role assignment | Attacker-controlled filenames influence amendment labeling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Denial of Service | Regex catastrophic backtracking on hostile text | mitigate | Line-anchored patterns with bounded quantifiers ({0,120}/{0,80} caps per RESEARCH sketches), no nested unbounded groups; adversarial timing test in Task 1 enforces sub-second matching |
| T-01-13 | Spoofing | Filename-based amendment labeling ("amend" in name of a non-amendment) | mitigate | Ladder ordering: form-text evidence always wins; filename rung fires ONLY when page 1 has no text layer, and result is explicitly marked amendment_evidence "filename" (unverified) — never presented as confirmed |
| T-01-14 | Repudiation (of analysis honesty) | Silent mis-sectioning of non-UCF packages | mitigate | classify_package never emits a classification without evidence + warnings; empty section list is a legal honest output; success criterion 2 verified on real corpus in plan 01-06 |
| T-01-SC | Tampering | package installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `uv run pytest tests/unit -q` green — all sectioning/classification heuristics covered by synthetic tests (corpus-free, CI-safe)
- Pure-library grep gate still holds across src/rfp_analyzer/pipeline/
</verification>

<success_criteria>
- UCF boundaries detected with TOC pages never mistaken for section starts (Pitfall 1 regression-tested)
- SF30 ladder labels amendments via form text or honest filename fallback (Pitfall 5 regression-tested)
- Four-way package classification always articulate (evidence + warnings), SF1449+L/M → partial_ucf nuance encoded
</success_criteria>

<output>
Create `.planning/phases/01-parsing-structure-foundation/01-05-SUMMARY.md` when done
</output>
