---
phase: 01-parsing-structure-foundation
plan: 06
type: execute
wave: 5
depends_on: [01-02, 01-05]
files_modified:
  - src/rfp_analyzer/pipeline/run.py
  - src/rfp_analyzer/cli.py
  - tests/integration/__init__.py
  - tests/integration/conftest.py
  - tests/integration/test_corpus_packages.py
  - README.md
autonomous: false
requirements: [PARS-01, PARS-02]

must_haves:
  truths:
    - "Running `rfp-analyzer parse <package-dir> --out <artifacts-dir>` on each corpus package writes a validating document_map.json AND prints a human-readable section tree to stdout (D-05: both artifacts, every run)"
    - "The primary corpus package run shows L/M/C boundaries with real page ranges, quality flags, and its SF30 labeled as amendment — all four phase success criteria observable in one report"
    - "The non-UCF corpus package is classified non_ucf_commercial with warnings — not silently mis-sectioned"
    - "Integration tests encode these outcomes against manifest.json and auto-skip when the corpus is absent (CI stays green)"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/run.py"
      provides: "run_pipeline(package_dir) -> DocumentMap — pure orchestration of discover → parse → quality → sectioning → classify with RunMetrics stage timings"
      exports: ["run_pipeline"]
    - path: "src/rfp_analyzer/cli.py"
      provides: "argparse parse subcommand: JSON artifact writing + stdout tree report + metrics summary (D-05/D-06)"
      exports: ["main"]
    - path: "tests/integration/test_corpus_packages.py"
      provides: "corpus-driven assertions for all four phase success criteria, keyed off tests/corpus/manifest.json"
  key_links:
    - from: "src/rfp_analyzer/cli.py"
      to: "src/rfp_analyzer/pipeline/run.py"
      via: "CLI wraps the pure library (only import direction allowed)"
      pattern: "from rfp_analyzer\\.pipeline\\.run import run_pipeline"
    - from: "src/rfp_analyzer/pipeline/run.py"
      to: "src/rfp_analyzer/pipeline/(parsing|quality|sectioning|classify)"
      via: "stage composition f(artifact) -> artifact"
      pattern: "apply_quality|build_section_tree|classify_package"
---

<objective>
Assemble the full Phase 1 pipeline into `run_pipeline`, implement the real CLI (D-05 dual output: canonical document_map.json + stdout section-tree report; D-06 shape: `parse <package-dir> --out <artifacts-dir>`), write corpus-driven integration tests keyed off manifest.json, and verify all four phase success criteria on the real 3-package corpus with a human checkpoint.

Purpose: This is where the phase goal becomes observable — the CLI harness run that ROADMAP success criteria 1-4 describe.
Output: Working end-to-end CLI; integration test suite; human-verified success criteria; Phase 2's input contract proven on real federal packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-parsing-structure-foundation/01-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@tests/corpus/manifest.json

<interfaces>
Stage functions composed by run_pipeline (read actual modules for exact signatures):
- parsing.discover.discover_files(package_dir) -> discovered entries (path, sha256, file_id, kind, prebuilt rejections)
- parsing.pdf.parse_pdf(path, sha256=, file_id=) -> ParsedFile; parsing.docx.parse_docx(...) -> ParsedFile
- quality.headers.apply_quality(file) -> ParsedFile (final page quality + cleaned text + stripped_headers)
- sectioning.tree.build_section_tree(file) -> list[SectionNode] (+ TOC page info)
- classify.forms.assign_doc_role(file) -> ParsedFile; classify.package.classify_package(files) -> (classification, evidence, warnings)
- metrics.RunMetrics(stage_timings, files_parsed, pages_parsed, pages_flagged, llm_* zeroed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline orchestration (run.py) and the real CLI with dual output</name>
  <files>src/rfp_analyzer/pipeline/run.py, src/rfp_analyzer/cli.py</files>
  <read_first>
    - src/rfp_analyzer/cli.py (current stub from plan 01-01 — replace the parse subcommand body, keep the entry-point shape)
    - src/rfp_analyzer/pipeline/parsing/discover.py, pdf.py, docx.py; src/rfp_analyzer/pipeline/quality/headers.py; src/rfp_analyzer/pipeline/sectioning/tree.py; src/rfp_analyzer/pipeline/classify/forms.py, package.py (exact signatures being composed)
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (System Architecture Diagram — stage order; D-05/D-06 in User Constraints — required outputs and CLI shape; Pattern 7 metrics bullet)
  </read_first>
  <action>
    Create `pipeline/run.py` exporting `run_pipeline(package_dir: Path) -> DocumentMap` — pure function, no printing, no HTTP/queue/CLI imports (the locked boundary): discover files; parse each per kind (rejections pass through as their prebuilt ParsedFile); apply_quality each; build_section_tree each ok file (attach to file.sections); assign_doc_role each; classify_package over all files → DocumentMap(package_name=package_dir.name, classification, classification_evidence, warnings, files, metrics). Time each stage with time.perf_counter into RunMetrics.stage_timings keyed "discover"/"parse"/"quality"/"sectioning"/"classify"; fill files_parsed, pages_parsed, pages_flagged (count of non-"ok" pages); LLM fields stay zero (D-09).
    Rewrite `cli.py` parse subcommand (D-06 shape: positional package_dir, `--out` default "artifacts"): validate package_dir exists/is a dir (exit 2 with message otherwise); call run_pipeline; create out dir; write `<out>/<package_name>/document_map.json` via model_dump_json(indent=2); render the stdout report (D-05): package name + classification + evidence lines; per file — filename, doc_role (amendment lines include the number and, when amendment_evidence is "filename", the literal suffix "(unverified — scanned, matched by filename)"), parse_status; section hierarchy as an indented tree with locator ranges ("SECTION L  pages 40-52" / "blocks 10-49" for DOCX); per-file quality flags aggregated into contiguous ranges ("pages 45-52: scanned image, no text layer" — D-03 surfaced-note format); package warnings block; metrics summary footer (stage timings, files/pages counts, pages flagged, "LLM cost: $0.00 (0 calls)" proving D-09 plumbing). Exit 0 on success, 1 when classification is "unknown" AND zero sections detected anywhere (machine-checkable honest-failure signal), 2 on usage errors. Keep report rendering functions in cli.py (presentation is not library code).
  </action>
  <verify>
    <automated>uv run rfp-analyzer parse tests/corpus/$(uv run python -c "import json; print(json.load(open('tests/corpus/manifest.json'))['packages'][0]['dir'])") --out artifacts 2>/dev/null | head -40 && uv run python -c "import json,glob; from rfp_analyzer.pipeline.models import DocumentMap; p=glob.glob('artifacts/*/document_map.json')[0]; DocumentMap.model_validate_json(open(p,encoding='utf-8').read()); print('map validates:', p)"</automated>
  </verify>
  <done>CLI produces both D-05 artifacts on a real corpus package; JSON validates against DocumentMap.</done>
  <acceptance_criteria>
    - `grep -c "import fastapi\|import httpx\|import requests\|from rfp_analyzer.cli" src/rfp_analyzer/pipeline/run.py` returns 0 (purity gate — CLI imports pipeline, never the reverse)
    - document_map.json round-trips through DocumentMap.model_validate_json with no ValidationError
    - stdout report contains the package classification literal and at least one "pages" locator range on the primary package
    - Metrics footer shows llm_calls 0 / $0.00 (D-09 visible)
    - `uv run rfp-analyzer parse nonexistent-dir` exits 2 with an error message
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corpus-driven integration tests keyed off manifest.json</name>
  <files>tests/integration/__init__.py, tests/integration/conftest.py, tests/integration/test_corpus_packages.py</files>
  <read_first>
    - tests/corpus/manifest.json (package dirs, roles, expected_classification, has_amendment, has_scanned_pages — written by plan 01-02)
    - tests/corpus/MANIFEST.md (per-package intel: which files are scanned, page counts — informs assertion specifics)
    - src/rfp_analyzer/pipeline/run.py (run_pipeline signature)
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 7 skipif bullet; Pitfall 7 — hostile-package assertions are the point)
  </read_first>
  <behavior>
    - Test 1 (SC1): for EVERY package in manifest.json, run_pipeline returns a DocumentMap containing one entry per corpus file in that dir, and every parse_status "ok" PDF file has pages with page numbers; the map serializes and re-validates
    - Test 2 (SC2 standard): the role "primary" package detects sections labeled L and M (and a C or sow_pws-role node) with page_start > 1 for L and M (TOC guard), classification == its manifest expected_classification
    - Test 3 (SC2 non-UCF): the role "non_ucf" package classifies non_ucf_commercial (per manifest) with len(warnings) >= 1 and no fabricated L/M sections
    - Test 4 (SC3): the role "hostile" package (has_scanned_pages) yields >= 1 page with quality in {"scanned","empty","low_text","gibberish"} and every such page has text == ""
    - Test 5 (SC4): each package with has_amendment true contains >= 1 file with doc_role "amendment"; scanned amendments carry amendment_evidence "filename"
    - Test 6: pipeline never crashes on any corpus package — run_pipeline completes and every file lands in exactly one parse_status of ok/failed/rejected
  </behavior>
  <action>
    Create tests/integration/conftest.py: module-level CORPUS_DIR = Path("tests/corpus"); load manifest.json when present; `pytest.mark.skipif` (or a conftest collection skip) when CORPUS_DIR lacks package subdirectories or manifest.json — CI has manifests but no binaries, so the skip condition must check for actual package dirs listed in the manifest, not just the manifest file. Provide a session-scoped fixture that runs run_pipeline once per package and caches the DocumentMap (3 packages × one parse each — keep the suite to one pipeline run per package). Implement the six behaviors as parametrized tests over manifest packages, reading expectations ONLY from manifest.json fields (dir, role, expected_classification, has_amendment, has_scanned_pages) so the tests are corpus-portable. If any assertion fails against the real corpus, this is corpus evidence per CONTEXT: tune the tunable constants (quality thresholds, TOC/heading heuristics from plans 01-04/01-05) — do NOT weaken assertions to pass; record every tuning change and its trigger in the SUMMARY (these feed Phase 2's understanding of the corpus).
  </action>
  <verify>
    <automated>uv run pytest tests/integration -q && uv run pytest tests/unit -q && uv run ruff check .</automated>
  </verify>
  <done>All six integration behaviors pass on the real corpus locally; unit suite + lint still green; tests skip cleanly without corpus.</done>
  <acceptance_criteria>
    - `uv run pytest tests/integration -q` passes locally with corpus present (>= 6 tests, 0 skipped)
    - Simulated CI check: temporarily renaming a package dir (or running with a tmp empty corpus path) makes the suite SKIP not FAIL — verified once, restored
    - Expectations sourced from manifest.json only (`grep -c "manifest.json" tests/integration/conftest.py` >= 1; no hardcoded solicitation numbers in test asserts)
    - TOC-guard assertion present: primary package L section page_start > 1
    - Any heuristic-constant tuning performed is listed in the SUMMARY with before/after values
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all four phase success criteria on the real corpus</name>
  <action>Pause for Ross to run the CLI on each of the three corpus packages and eyeball the stdout section-tree reports against ROADMAP success criteria 1-4 (D-05: the human-readable report exists precisely for this). Steps in how-to-verify below.</action>
  <what-built>The complete Phase 1 pipeline: `rfp-analyzer parse` runs end-to-end on all three corpus packages, writing document_map.json artifacts and printing section-tree reports. Integration tests assert all four success criteria automatically; this checkpoint is the human eyeball of the same evidence (D-05's stated purpose for the stdout report).</what-built>
  <how-to-verify>
    From the repo root, run each package (dirs per tests/corpus/MANIFEST.md):
      1. `uv run rfp-analyzer parse tests/corpus/<primary-dir> --out artifacts`
         Expect: classification full_ucf (or partial_ucf with a sensible warning); SECTION L / M / C (or SOW/PWS) in the tree with plausible page ranges NOT starting at page 1; the SF30 file labeled "amendment"; metrics footer with $0.00 LLM cost.
      2. `uv run rfp-analyzer parse tests/corpus/<hostile-dir> --out artifacts`
         Expect: the known-scanned pages surfaced as a range note ("pages X-Y: scanned image, no text layer"); if the scanned file is the SF30, it's labeled amendment "(unverified — scanned, matched by filename)".
      3. `uv run rfp-analyzer parse tests/corpus/<non-ucf-dir> --out artifacts`
         Expect: classification non_ucf_commercial with evidence (combined-synopsis/SF1449 trigger named) and warnings — NO invented L/M sections.
      4. Spot-check one artifacts/*/document_map.json in an editor: real text in ok pages, "" on scanned pages, sections with locators.
      5. Sanity-check the section page ranges against the actual PDFs (open the base solicitation, confirm SECTION L truly starts near the reported page).
  </how-to-verify>
  <resume-signal>Type "approved" if all four success criteria look right, or describe what's off (wrong boundaries, missed scanned pages, mislabeled amendment, dishonest classification) — failures route back into heuristic tuning, not scope reduction</resume-signal>
</task>

<task type="auto">
  <name>Task 4: README usage docs, final commit, CI green</name>
  <files>README.md</files>
  <read_first>
    - README.md (current stub from plan 01-01)
    - src/rfp_analyzer/cli.py (final CLI flags/behavior to document)
  </read_first>
  <action>
    Update README.md: add a Usage section (install via `uv sync`, run via `uv run rfp-analyzer parse <package-dir> --out <artifacts-dir>`, what the two outputs are), an Architecture note (pure `pipeline/` library, stage diagram summary, document_map.json as the downstream contract), a Test Corpus note (corpus gitignored; see tests/corpus/MANIFEST.md; integration tests auto-skip without it), and update the status line to "Phase 1 complete: parsing & structure foundation". Commit all plan 01-06 work (conventional commits), push, confirm `gh run list --limit 1` shows success.
  </action>
  <verify>
    <automated>gh run list --limit 1 --json conclusion -q '.[0].conclusion'</automated>
  </verify>
  <done>README documents real usage; CI green on the final push.</done>
  <acceptance_criteria>
    - README contains the literal command `rfp-analyzer parse` and mentions document_map.json
    - `gh run list --limit 1 --json conclusion -q '.[0].conclusion'` outputs success
    - `git status --porcelain` clean after push (no stray artifacts committed; artifacts/ is gitignored)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus packages → full pipeline | Hostile multi-file packages exercise every stage in one process |
| CLI args → filesystem writes | --out path controls where artifacts are written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Denial of Service | Whole-package resource exhaustion (3 × 100-300 page packages) | mitigate | Per-file isolation + page.close() already enforced upstream; stage timings in RunMetrics make slow stages visible; single-package-per-invocation CLI bounds working set |
| T-01-16 | Tampering | Artifact writes outside intended --out dir (package/file names influencing paths) | mitigate | Output path = out_dir / package_dir.name / "document_map.json" only — file-derived names never used in output paths; out dir created with pathlib mkdir(parents=True) |
| T-01-17 | Repudiation | Report claiming success on garbage results | mitigate | Exit code 1 on unknown+zero-sections; human-verify checkpoint eyeballs real PDFs against reported ranges; integration tests pin the four success criteria to manifest expectations |
| T-01-SC | Tampering | package installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `uv run pytest tests/unit tests/integration -q` fully green locally with corpus; integration skips (not fails) without corpus
- All four ROADMAP Phase 1 success criteria observed by a human on real packages (checkpoint)
- Public repo CI green on final push
</verification>

<success_criteria>
- SC1: CLI on a multi-file package → document map with section hierarchy + page numbers for every file (JSON validates, report renders)
- SC2: UCF boundaries correct on the primary package; non-UCF package flagged non_ucf_commercial with warnings, never mis-sectioned
- SC3: scanned/low-quality pages caught and surfaced as explicit range notes
- SC4: SF30 amendment files identified and labeled (form-text or honest filename fallback)
</success_criteria>

<output>
Create `.planning/phases/01-parsing-structure-foundation/01-06-SUMMARY.md` when done
</output>
