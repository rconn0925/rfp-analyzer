---
phase: 01-parsing-structure-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/rfp_analyzer/pipeline/parsing/__init__.py
  - src/rfp_analyzer/pipeline/parsing/discover.py
  - src/rfp_analyzer/pipeline/parsing/pdf.py
  - src/rfp_analyzer/pipeline/parsing/docx.py
  - tests/unit/conftest.py
  - tests/unit/test_discover.py
  - tests/unit/test_pdf.py
  - tests/unit/test_docx.py
autonomous: true
requirements: [PARS-01]

must_haves:
  truths:
    - "A directory of mixed files yields one ParsedFile per file: PDFs parsed page-by-page, DOCX parsed block-by-block, .doc and unknown extensions rejected with explicit status — never a crash"
    - "A malformed PDF produces parse_status 'failed' with an error message and the run continues (per-file isolation)"
    - "PDF page text carries 1-indexed page numbers; DOCX blocks carry 0-indexed document-order ordinals — no synthesized DOCX page numbers"
    - "Every file gets a stable identity: sha256 + file_id slug"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/parsing/discover.py"
      provides: "discover_files(package_dir) — extension allowlist, rejection records, size guard, path containment"
      exports: ["discover_files"]
    - path: "src/rfp_analyzer/pipeline/parsing/pdf.py"
      provides: "parse_pdf(path) -> ParsedFile with per-page text + raw metrics, page.close() memory management"
      exports: ["parse_pdf"]
    - path: "src/rfp_analyzer/pipeline/parsing/docx.py"
      provides: "parse_docx(path) -> ParsedFile with ordered BlockInfo blocks, zip-bomb guard"
      exports: ["parse_docx"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/parsing/pdf.py"
      to: "src/rfp_analyzer/pipeline/models.py"
      via: "constructs ParsedFile/PageInfo"
      pattern: "from rfp_analyzer\\.pipeline\\.models import"
    - from: "src/rfp_analyzer/pipeline/parsing/docx.py"
      to: "src/rfp_analyzer/pipeline/models.py"
      via: "constructs ParsedFile/BlockInfo"
      pattern: "BlockInfo"
---

<objective>
Build the per-file parsing layer of the pure pipeline library: file discovery with hostile-input guards, PDF parsing via pdfplumber (per-page text, raw quality metrics, memory-safe), and DOCX parsing via python-docx (document-order blocks with ordinals). All failure modes produce explicit per-file statuses, never crashed runs.

Purpose: This is the raw-material stage of PARS-01 — everything downstream (quality, sectioning, classification) consumes ParsedFile objects produced here.
Output: `rfp_analyzer.pipeline.parsing` package with discover/pdf/docx modules and unit tests running on synthetic fixtures (no corpus dependency — runs in CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-parsing-structure-foundation/01-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py

<interfaces>
From src/rfp_analyzer/pipeline/models.py (created by plan 01-01 per RESEARCH Pattern 6 — read the actual file for exact signatures):
- ParsedFile: file_id, filename, sha256, file_type ("pdf"|"docx"), parse_status ("ok"|"failed"|"rejected"), error, doc_role (default "unknown"), amendment_number, amendment_evidence, page_count, pages: list[PageInfo], blocks: list[BlockInfo], stripped_headers, sections, first_page_layout_text
- PageInfo: page_number (1-indexed), quality (use "pending" at parse time), char_count, metrics: dict[str, float], text (raw at this stage; quality stage cleans it)
- BlockInfo: ordinal (0-indexed), kind ("paragraph"|"table"), text, style, table cells
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: File discovery with identity, allowlist, and hostile-input guards</name>
  <files>src/rfp_analyzer/pipeline/parsing/__init__.py, src/rfp_analyzer/pipeline/parsing/discover.py, tests/unit/test_discover.py, tests/unit/conftest.py</files>
  <read_first>
    - src/rfp_analyzer/pipeline/models.py (ParsedFile fields — rejection records are ParsedFile instances)
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md ("Stable file identity" snippet; Security Domain V5 row — extension allowlist, pathlib containment; Known Threat Patterns — size sanity check)
  </read_first>
  <behavior>
    - Test 1: discover_files on a dir with a.pdf, b.docx, c.doc, d.txt returns 4 entries; a/b classified pdf/docx for parsing; c and d yield ParsedFile(parse_status="rejected") with error messages naming the reason (".doc legacy Word not supported" for c)
    - Test 2: every returned entry has sha256 (64 hex chars) and file_id formatted `{sha256[:12]}-{sanitized-stem}` (lowercase, non-alphanumerics collapsed to "-", stem truncated to 40 chars)
    - Test 3: a file exceeding the size cap constant yields parse_status "rejected" with an error mentioning size (create the test file sparsely or monkeypatch the constant down to a few KB)
    - Test 4: a symlink pointing outside package_dir (or a path whose resolve() is not under package_dir.resolve()) is rejected, not followed (skip the symlink half on Windows if symlink creation needs privileges — the resolve-containment check is testable via monkeypatch either way)
    - Test 5: subdirectories are not recursed into silently — files in nested dirs ARE discovered (SAM.gov zips often nest) but their filename records the relative path
  </behavior>
  <action>
    Create `discover.py` exporting `discover_files(package_dir: Path) -> list[DiscoveredFile]` where DiscoveredFile is a small dataclass/NamedTuple pairing path + sha256 + file_id + kind ("pdf"|"docx"|"rejected") + a prebuilt rejection ParsedFile when kind is "rejected". Rules: iterate `package_dir.rglob("*")` files only; allowlist by lowercase suffix — ".pdf" → pdf, ".docx" → docx; ".doc" → rejected with explicit legacy-Word message (CLAUDE.md constraint); anything else → rejected "unsupported file type"; per-file size guard constant `MAX_FILE_BYTES = 250 * 1024 * 1024` (module-level, corpus-tunable) → rejected with size message; path containment — `path.resolve().is_relative_to(package_dir.resolve())` must hold or the file is rejected "outside package directory" (defends traversal via symlinks/junctions). sha256 via streaming hashlib (1 MB chunks) exactly per RESEARCH "Stable file identity"; file_id = first 12 hex chars + "-" + sanitized stem (regex collapse of non-[a-z0-9] runs to "-", 40-char cap).
    In tests/unit/conftest.py add two fixture factories used by this and later test files: `make_minimal_pdf(path, pages: list[str])` — writes a small valid single-xref PDF by assembling raw PDF syntax bytes (catalog, pages tree, one content stream per page using uncompressed `BT /F1 12 Tf (text) Tj ET` with Helvetica; pdfminer parses uncompressed streams fine) — and `make_docx(path, paragraphs, tables=None)` using python-docx `Document().save()`. These synthetic fixtures keep unit tests corpus-free so CI stays green.
    Write tests RED first, then implement GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_discover.py -x -q</automated>
  </verify>
  <done>discover_files handles pdf/docx/doc/other/oversize/outside-dir with the exact statuses above; identity is stable across runs.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.parsing.discover import discover_files, MAX_FILE_BYTES"` exits 0
    - Rejected .doc entry's error string contains "doc" and "not supported" (behavior test asserts exact substring)
    - Same file hashed twice returns identical file_id (test asserts determinism)
    - `grep -rc "import requests\|import httpx\|import fastapi" src/rfp_analyzer/pipeline/` returns 0 for all files (pure-library boundary)
    - pytest exit 0, >= 5 tests in test_discover.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PDF parsing with pdfplumber — per-page text, raw metrics, memory safety, failure isolation</name>
  <files>src/rfp_analyzer/pipeline/parsing/pdf.py, tests/unit/test_pdf.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 1 "Per-file parse with isolated failure" — the exact open/pages/close shape; Pitfall 2, Pitfall 3; "pdfplumber words with positions" snippet; Assumption A3 — verify page.images attr at implementation)
    - src/rfp_analyzer/pipeline/models.py (PageInfo/ParsedFile construction)
    - tests/unit/conftest.py (make_minimal_pdf fixture from Task 1)
  </read_first>
  <behavior>
    - Test 1: parse_pdf on a 3-page synthetic PDF returns ParsedFile(parse_status="ok", file_type="pdf", page_count=3) with pages[i].page_number == i+1 and each page's text containing its fixture string
    - Test 2: parse_pdf on a garbage file (b"%PDF-1.4 then random bytes") returns parse_status="failed" with non-empty error and pages == [] — no exception escapes
    - Test 3: each PageInfo.metrics contains keys "cid_count" and "replacement_char_count" and PageInfo has char_count and an image-presence signal recorded in metrics ("has_images" as 0.0/1.0) — computed from raw extraction
    - Test 4: pages[*].quality == "pending" for every page (quality stage owns final status)
    - Test 5: first_page_layout_text is populated (non-None) for a parseable PDF and differs source-wise from default-mode text (layout=True on page 1 only)
  </behavior>
  <action>
    Create `pdf.py` exporting `parse_pdf(path: Path, *, sha256: str, file_id: str) -> ParsedFile` following RESEARCH Pattern 1 exactly: `with pdfplumber.open(path)`, iterate `pdf.pages`, per page call `page.extract_text(x_tolerance=3, y_tolerance=3) or ""`, record char_count from `len(page.chars)`, image presence from `page.images` (Assumption A3 — verify the attribute exists on first run; fall back to `page.objects.get("image")` if not), count `(cid:` occurrences and U+FFFD chars in extracted text into PageInfo.metrics, then `page.close()` after consuming each page (Pitfall 3 memory guard — never retain Page objects, only your own PageInfo). For page 1 only, additionally call `page.extract_text(layout=True)` BEFORE close and store it as ParsedFile.first_page_layout_text (feeds plan 01-05 SF-form field extraction; do this before page.close() invalidates caches). Whole function body wrapped so ANY exception (pdfminer raises varied types — catch Exception) returns ParsedFile(parse_status="failed", error=str(exc), pages=[]) per Pitfall 2. Do NOT use layout=True for body pages (RESEARCH anti-pattern: slower + artificial whitespace). quality="pending" on every PageInfo; text stored raw (quality stage cleans in plan 01-04).
    Tests use conftest fixtures; include a wall-clock sanity assertion that a 3-page synthetic parse completes under 10 seconds (guards accidental quadratic behavior, not a hard DoS control).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pdf.py -x -q</automated>
  </verify>
  <done>parse_pdf produces ok/failed ParsedFiles per the behaviors; no unhandled exceptions on garbage input.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.parsing.pdf import parse_pdf"` exits 0
    - `grep -c "page.close()" src/rfp_analyzer/pipeline/parsing/pdf.py` >= 1 (memory guard present)
    - `grep -c "layout=True" src/rfp_analyzer/pipeline/parsing/pdf.py` == 1 (page-1 only, not body pages)
    - Garbage-input test passes with parse_status == "failed" and error != None
    - pytest exit 0, >= 5 tests in test_pdf.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DOCX parsing with python-docx — ordered blocks, ordinal locators, zip-bomb guard</name>
  <files>src/rfp_analyzer/pipeline/parsing/docx.py, tests/unit/test_docx.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 2 "DOCX document-order block extraction" — iter_inner_content, style names, RenderedPageBreak caveat; Pitfall 4)
    - src/rfp_analyzer/pipeline/models.py (BlockInfo fields)
    - tests/unit/conftest.py (make_docx fixture)
  </read_first>
  <behavior>
    - Test 1: a DOCX with paragraphs P0, P1, a table, then P2 yields blocks with ordinals 0,1,2,3 in document order; block 2 has kind "table" with cell text preserved as rows of strings; paragraphs carry style names (e.g. "Normal", "Heading 1")
    - Test 2: parse_docx returns ParsedFile(file_type="docx", page_count=None, pages=[]) — NO synthesized page numbers anywhere (RESEARCH anti-pattern)
    - Test 3: a corrupt file with .docx extension (random bytes) returns parse_status="failed" with error, no exception
    - Test 4: a zip whose declared uncompressed size exceeds the guard (craft a small zip claiming/containing a huge deflated member, e.g. compress 200MB of zeros → tiny file; monkeypatch the guard constant down if needed) returns parse_status="failed" with an error mentioning decompression/size — python-docx is never invoked on it
  </behavior>
  <action>
    Create `docx.py` exporting `parse_docx(path: Path, *, sha256: str, file_id: str) -> ParsedFile`. Pre-open guard (DOCX files are zip archives — decompression-bomb surface): open with stdlib `zipfile.ZipFile`, sum `info.file_size` over infolist(); if total uncompressed size > `MAX_DOCX_UNCOMPRESSED = 1 * 1024**3` (1 GiB, module constant) or compression ratio (uncompressed/compressed) > `MAX_COMPRESSION_RATIO = 200`, return parse_status="failed" with an explicit bomb-suspect message before python-docx touches it. Then Pattern 2: `Document(str(path))`, enumerate `doc.iter_inner_content()` (Iterator[Paragraph | Table], document order); Paragraph → BlockInfo(ordinal=i, kind="paragraph", text=item.text, style=item.style.name); Table → BlockInfo(ordinal=i, kind="table", table=[[cell.text for cell in row.cells] for row in item.rows]). Do NOT synthesize page numbers (RenderedPageBreak unreliable per Assumption A6) — locators for DOCX are block ordinals, period. Wrap in try/except Exception → parse_status="failed" (python-docx raises on malformed packages). Note for downstream: heading detection from style names AND text heuristics happens in sectioning (plan 01-05, Pitfall 4) — this module only records style names faithfully.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_docx.py -x -q</automated>
  </verify>
  <done>parse_docx yields ordered typed blocks with faithful styles; bombs and corrupt files fail explicitly pre-parse or in-parse.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.parsing.docx import parse_docx, MAX_DOCX_UNCOMPRESSED"` exits 0
    - `grep -c "zipfile" src/rfp_analyzer/pipeline/parsing/docx.py` >= 1 (bomb guard precedes Document())
    - `grep -c "page_number\|page_count=[0-9]" src/rfp_analyzer/pipeline/parsing/docx.py` returns 0 (no fabricated DOCX pages)
    - `grep -c "iter_inner_content" src/rfp_analyzer/pipeline/parsing/docx.py` >= 1
    - pytest exit 0, >= 4 tests in test_docx.py; full suite `uv run pytest tests/unit -q` still green; commit + push, CI green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus files → parser | Hostile/malformed PDF and DOCX bytes cross into pdfplumber/python-docx |
| package filenames → filesystem paths | Attacker-controlled names could point outside the package dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | parse_pdf on pathological PDFs (deep object graphs, hangs) | mitigate | Per-file try/except isolation (Pitfall 2); page.close() memory cap (Pitfall 3); MAX_FILE_BYTES pre-parse size guard. No hard wall-clock timeout in Phase 1 — documented accepted residual for a local CLI (RESEARCH ASSUMED note); MUST revisit before Phase 4 web upload |
| T-01-07 | Denial of Service | DOCX zip bomb via python-docx | mitigate | zipfile uncompressed-size (1 GiB) + compression-ratio (200x) guard before Document() opens the archive |
| T-01-08 | Tampering / Elevation | Path traversal via symlinks or crafted names in package dir | mitigate | discover.py resolve()-containment check: any file not under package_dir.resolve() is rejected; pathlib only, no string path concat |
| T-01-09 | Denial of Service | Memory exhaustion on 100–300 page PDFs | mitigate | page.close() after each page; retain only PageInfo, never Page objects |
| T-01-SC | Tampering | package installs | accept | No new packages — pdfplumber/python-docx already installed and audited in plan 01-01 |
</threat_model>

<verification>
- `uv run pytest tests/unit -q` green with all parsing tests (no corpus required)
- Pure-library grep gate: no HTTP/queue/CLI imports anywhere under src/rfp_analyzer/pipeline/
- CI green after push
</verification>

<success_criteria>
- Mixed-content directory → per-file ParsedFile records with correct statuses, identities, page numbers (PDF) and block ordinals (DOCX)
- Malformed inputs (garbage PDF, corrupt DOCX, zip bomb, oversize, traversal) all produce explicit failed/rejected records, zero crashes
- Raw per-page metrics captured for plan 01-04's quality gates
</success_criteria>

<output>
Create `.planning/phases/01-parsing-structure-foundation/01-03-SUMMARY.md` when done
</output>
