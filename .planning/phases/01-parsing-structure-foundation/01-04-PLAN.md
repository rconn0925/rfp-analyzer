---
phase: 01-parsing-structure-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - src/rfp_analyzer/pipeline/quality/__init__.py
  - src/rfp_analyzer/pipeline/quality/gates.py
  - src/rfp_analyzer/pipeline/quality/headers.py
  - tests/unit/test_quality_gates.py
  - tests/unit/test_headers.py
autonomous: true
requirements: [PARS-01]

must_haves:
  truths:
    - "Every PDF page gets a final quality status (ok/scanned/empty/low_text/gibberish) — 'pending' never survives the quality stage"
    - "Scanned image-only pages are flagged, their text emptied, and they are excluded from downstream usable text (D-03: flag-and-surface, no OCR)"
    - "Running headers/footers (e.g. 'W912DY-26-R-0012 — Page 47') are stripped from stored page text and the stripped patterns are recorded on the file"
    - "All thresholds are named module-level constants, tunable against the corpus without touching logic"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/quality/gates.py"
      provides: "compute_page_metrics + classify_page per RESEARCH Pattern 5 thresholds"
      exports: ["classify_page", "compute_page_metrics"]
    - path: "src/rfp_analyzer/pipeline/quality/headers.py"
      provides: "cross-page frequency header/footer detection + apply_quality file-level stage function"
      exports: ["apply_quality"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/quality/headers.py"
      to: "src/rfp_analyzer/pipeline/models.py"
      via: "rewrites PageInfo.text/quality and ParsedFile.stripped_headers"
      pattern: "stripped_headers"
---

<objective>
Implement the per-page quality gates and header/footer stripping (D-03): compute metrics from raw parse output, classify each page (ok/scanned/empty/low_text/gibberish), empty the text of non-ok pages so garbage never flows downstream, and strip running headers/footers from ok pages before sectioning consumes them.

Purpose: Success criterion 3 — low-quality/scanned pages are caught and surfaced, never silent garbage. Also prevents Pitfall 6 (header noise contaminating Phase 2 requirement extraction).
Output: `rfp_analyzer.pipeline.quality` package exposing `apply_quality(ParsedFile) -> ParsedFile` as the stage function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-parsing-structure-foundation/01-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/parsing/pdf.py

<interfaces>
From plan 01-03 (read actual files for exact shapes):
- parse_pdf produces PageInfo(quality="pending", text=raw_text, char_count, metrics={"cid_count": n, "replacement_char_count": n, "has_images": 0.0|1.0, ...})
- DOCX ParsedFiles have blocks, not pages — quality gates are per-PDF-page; DOCX passes through apply_quality unchanged except stage bookkeeping
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-page metrics and status classification with tunable thresholds</name>
  <files>src/rfp_analyzer/pipeline/quality/__init__.py, src/rfp_analyzer/pipeline/quality/gates.py, tests/unit/test_quality_gates.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 5 — exact metric definitions and initial thresholds; Pitfall 7 — calibrate on hostile data; D-03 in User Constraints)
    - src/rfp_analyzer/pipeline/parsing/pdf.py (what raw metrics parsing already computed — reuse, don't recompute)
    - src/rfp_analyzer/pipeline/models.py (PageInfo.quality literal values)
  </read_first>
  <behavior>
    - Test 1: page with char_count 5 and has_images → "scanned"; char_count 5 and no images → "empty"
    - Test 2: page with 100 chars and images → "low_text" (below LOW_TEXT_CHARS=150 with images)
    - Test 3: page of symbol soup (alpha_ratio < 0.5) → "gibberish"; page with cid_count/char_count > 0.02 → "gibberish"; page with 6 U+FFFD chars → "gibberish"
    - Test 4: normal prose page → "ok" with metrics dict containing alpha_ratio and printable_ratio as floats in [0,1]
    - Test 5: thresholds are importable named constants (SCANNED_MAX_CHARS=20, LOW_TEXT_CHARS=150, GIBBERISH_ALPHA_RATIO=0.5, GIBBERISH_CID_RATIO=0.02, GIBBERISH_REPLACEMENT_MAX=5) — test asserts classify_page respects a monkeypatched constant (proving they're read at call time or injectable via a QualityThresholds dataclass with defaults)
  </behavior>
  <action>
    Create `gates.py` with: `compute_page_metrics(text: str) -> dict[str, float]` computing alpha_ratio (alphabetic / non-space chars, 0.0 when denominator 0), printable_ratio, cid_count (count of "(cid:" substring), replacement_char_count (U+FFFD count) — merging with metrics already captured at parse time; and `classify_page(page: PageInfo) -> Literal[...]` implementing RESEARCH Pattern 5 status rules exactly: scanned (char_count < SCANNED_MAX_CHARS and has_images), empty (char_count < SCANNED_MAX_CHARS, no images), low_text (char_count < LOW_TEXT_CHARS with images), gibberish (alpha_ratio < GIBBERISH_ALPHA_RATIO or cid ratio > GIBBERISH_CID_RATIO or replacement chars > GIBBERISH_REPLACEMENT_MAX), else ok. Prefer a `QualityThresholds` frozen dataclass with the five defaults, passed as an optional parameter defaulting to module-level DEFAULT_THRESHOLDS — this is the corpus-tuning knob CONTEXT locks as discretionary-but-tunable. Docstring each threshold with its RESEARCH assumption tag (A1) so future tuning has provenance.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_quality_gates.py -x -q</automated>
  </verify>
  <done>All five status paths reachable and tested; thresholds injectable.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.quality.gates import classify_page, compute_page_metrics, DEFAULT_THRESHOLDS"` exits 0
    - Each of the five statuses ("ok","scanned","empty","low_text","gibberish") is asserted at least once in test_quality_gates.py (grep for each literal in the test file returns >= 1)
    - alpha_ratio of "" is 0.0, not ZeroDivisionError (explicit test)
    - pytest exit 0, >= 5 tests
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Header/footer stripping and the apply_quality stage function</name>
  <files>src/rfp_analyzer/pipeline/quality/headers.py, tests/unit/test_headers.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 5 header/footer paragraph — normalization recipe, 40% frequency threshold, top/bottom line bands; Pitfall 6)
    - src/rfp_analyzer/pipeline/quality/gates.py (Task 1 — classify_page consumed here)
    - src/rfp_analyzer/pipeline/models.py (ParsedFile.stripped_headers, PageInfo.text semantics: cleaned, "" when not ok)
  </read_first>
  <behavior>
    - Test 1: a 10-page ParsedFile where 9 pages start with "SOLICITATION W912DY-26-R-0012" and end with "Page N of 10" → both patterns detected; cleaned page text contains neither; ParsedFile.stripped_headers records both normalized patterns
    - Test 2: normalization folds page-number variance — "Page 3 of 120" and "Page 47 of 120" count as ONE pattern (digits stripped before frequency counting)
    - Test 3: a line appearing on only 2 of 10 pages (20% < 40% threshold) is NOT stripped
    - Test 4: apply_quality on a ParsedFile with one scanned page → that page's quality == "scanned" and text == ""; ok pages keep cleaned text; no page remains "pending"
    - Test 5: apply_quality on a DOCX ParsedFile (blocks, no pages) returns it unchanged (no crash, no fabricated page quality)
    - Test 6: apply_quality on a parse_status "failed" ParsedFile passes it through untouched
  </behavior>
  <action>
    Create `headers.py` with: `detect_running_lines(pages) -> set[str]` implementing RESEARCH Pattern 5 — take first 2 and last 2 physical lines of each OK-classified page's text, normalize (strip all digits, collapse whitespace runs to single space, uppercase, strip), count frequency across OK pages, return normalized keys appearing on >= HEADER_FREQ_THRESHOLD=0.4 of OK pages (named constant, corpus-tunable, assumption A2); and `apply_quality(file: ParsedFile, thresholds=DEFAULT_THRESHOLDS) -> ParsedFile` — the stage function: skip non-"ok" parse_status files; for PDF files, first classify every page via gates.classify_page, then detect running lines across ok pages, then rewrite each PageInfo: non-ok pages get text="" (D-03: excluded from usable text) keeping their metrics for reporting; ok pages get text with any line whose normalized form is in the running set removed; record sorted stripped patterns in ParsedFile.stripped_headers. DOCX files pass through (block-level quality is out of scope — headers/footers live in docx section properties python-docx doesn't interleave into body blocks). Return a new/updated ParsedFile with zero "pending" statuses remaining. Order matters: classification BEFORE stripping (stripping must not change scanned/empty determinations); stripping uses only OK pages for frequency counting (scanned pages have no text to vote with).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_headers.py -x -q && uv run pytest tests/unit -q</automated>
  </verify>
  <done>apply_quality is the single quality-stage entry point; behaviors 1-6 pass; full unit suite green.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.quality.headers import apply_quality, HEADER_FREQ_THRESHOLD"` exits 0
    - Post-apply_quality assertion in tests: `all(p.quality != "pending" for p in file.pages)` for PDF files
    - Digit-stripping normalization proven by Test 2 (single pattern, not N patterns)
    - `grep -c "ocr\|OCR\|tesseract\|docling" src/rfp_analyzer/pipeline/quality/*.py` returns 0 (D-03/D-04: no OCR in Phase 1)
    - pytest full unit suite exit 0; commit + push, CI green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw parsed text → quality stage | Hostile extracted text (huge pages, pathological unicode) processed by pure-Python metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Denial of Service | metric computation over very large page text | mitigate | All metrics are single-pass O(n) string scans (count/sum), no regex backtracking; page text already bounded by MAX_FILE_BYTES upstream |
| T-01-11 | Information Disclosure (integrity of downstream analysis) | Garbage/scanned text silently reaching Phase 2 | mitigate | This plan IS the mitigation: non-ok pages emptied + statuses recorded; "pending" cannot survive apply_quality (tested) |
| T-01-SC | Tampering | package installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `uv run pytest tests/unit -q` green (quality tests are synthetic — no corpus needed, CI-safe)
- Optional local sanity if corpus present (plan 01-02 done): run apply_quality over the hostile package's parsed files and confirm known-scanned pages classify as "scanned" — record findings in SUMMARY for threshold tuning evidence
</verification>

<success_criteria>
- Every PDF page carries a final quality status; scanned/empty/low_text/gibberish pages surfaced with empty text (D-03)
- Running headers/footers stripped before sectioning, patterns recorded for auditability (Pitfall 6)
- All thresholds named, documented, and injectable for corpus tuning
</success_criteria>

<output>
Create `.planning/phases/01-parsing-structure-foundation/01-04-SUMMARY.md` when done
</output>
