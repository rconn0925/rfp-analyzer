---
phase: 01-parsing-structure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - .python-version
  - README.md
  - .github/workflows/ci.yml
  - src/rfp_analyzer/__init__.py
  - src/rfp_analyzer/cli.py
  - src/rfp_analyzer/pipeline/__init__.py
  - src/rfp_analyzer/pipeline/models.py
  - src/rfp_analyzer/pipeline/metrics.py
  - tests/unit/test_models.py
autonomous: true
requirements: [PARS-01, PARS-02]

must_haves:
  truths:
    - "uv sync + uv run pytest succeed locally from a clean checkout"
    - "The repo is public on GitHub under rconn0925 (D-07: public repo from day 1) with CI running lint + format-check + unit tests on push (D-08: uv/ruff/pytest + GitHub Actions from Phase 1)"
    - "DocumentMap schema round-trips through JSON and is importable as the Phase 2 contract"
    - "Corpus binaries placed under tests/corpus/ are invisible to git before any corpus file ever lands"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed project; locked deps pdfplumber/python-docx/pydantic; ruff config; rfp-analyzer script entry point"
    - path: "src/rfp_analyzer/pipeline/models.py"
      provides: "DocumentMap, ParsedFile, PageInfo, BlockInfo, SectionNode, PageSpan, BlockSpan, Locator — the versioned Phase 2 contract"
      min_lines: 80
    - path: "src/rfp_analyzer/pipeline/metrics.py"
      provides: "RunMetrics scaffolding per D-09 with zeroed LLM fields"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions: uv sync --locked, ruff check, ruff format --check, pytest tests/unit"
    - path: ".gitignore"
      provides: "corpus exclusion (tests/corpus/*) with MANIFEST.md and manifest.json exceptions"
  key_links:
    - from: "pyproject.toml"
      to: "src/rfp_analyzer/cli.py"
      via: "[project.scripts] rfp-analyzer entry point"
      pattern: "rfp-analyzer\\s*=\\s*\"rfp_analyzer\\.cli:main\""
    - from: "src/rfp_analyzer/pipeline/models.py"
      to: "src/rfp_analyzer/pipeline/metrics.py"
      via: "DocumentMap.metrics field typed RunMetrics"
      pattern: "RunMetrics"
---

<objective>
Stand up the project skeleton: uv-managed Python 3.12 src-layout package, locked dependencies, ruff/pytest tooling, the Pydantic document-map schema that is Phase 2's input contract, cost-instrumentation scaffolding (D-09), a public GitHub repo (D-07), and CI (D-08). The .gitignore corpus exclusion MUST exist before plan 01-02 downloads any corpus binaries.

Purpose: Every later plan imports these models and runs under this tooling; the public repo with clean history is itself a portfolio deliverable.
Output: Installable `rfp_analyzer` package with `pipeline/` pure-library subpackage, passing CI on a public GitHub repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-parsing-structure-foundation/01-CONTEXT.md
@.planning/phases/01-parsing-structure-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install uv, initialize the package, lock dependencies, configure tooling and .gitignore</name>
  <files>pyproject.toml, uv.lock, .python-version, .gitignore, README.md, src/rfp_analyzer/__init__.py, src/rfp_analyzer/cli.py, src/rfp_analyzer/pipeline/__init__.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 7 "Scaffolding + CI", "Environment Availability", "Standard Stack" — install commands, ruff rule set, gitignore pattern)
    - CLAUDE.md (locked stack versions, prohibited libraries)
    - .gitignore if present (repo currently has planning commits only; merge, don't clobber)
  </read_first>
  <action>
    1. Check `uv --version`; if absent, install via `winget install astral-sh.uv` (winget verified present per RESEARCH Environment Availability), then re-open shell path or invoke uv by full path (`$env:USERPROFILE\.local\bin\uv.exe` is the winget/standalone default). Verify `uv --version` succeeds before proceeding.
    2. In the repo root (already a git repo with .planning/ and CLAUDE.md — uv init works in non-empty dirs), run `uv init --package --name rfp-analyzer`, producing src layout with module `rfp_analyzer`. Pin Python: `uv python pin 3.12` (writes .python-version; system Python is 3.14 — uv fetches a managed 3.12). Set `requires-python = ">=3.12"` in pyproject.toml.
    3. Add locked deps per RESEARCH Standard Stack: `uv add pdfplumber python-docx pydantic` then `uv add --dev pytest pytest-cov ruff`. Do NOT add typer/click (argparse decision), PyMuPDF (AGPL — prohibited), or Docling (D-04 deferred). All 6 packages passed the slopcheck legitimacy audit in RESEARCH.md (6/6 OK) — no install checkpoints required.
    4. In pyproject.toml add: `[project.scripts]` with `rfp-analyzer = "rfp_analyzer.cli:main"`; `[tool.ruff]` with `line-length = 100`; `[tool.ruff.lint]` with `select = ["E", "F", "I", "UP", "B", "SIM"]`.
    5. Create `src/rfp_analyzer/cli.py` as a minimal argparse stub: a `main()` function with an argparse parser named `rfp-analyzer` and a `parse` subcommand accepting positional `package_dir` and `--out` (default `artifacts`) that currently prints "not yet implemented" and exits 2. Plan 01-06 replaces the body; the entry point must resolve now so `uv run rfp-analyzer --help` works.
    6. Create empty `src/rfp_analyzer/pipeline/__init__.py` (pure-library subpackage root — no HTTP/queue/CLI imports ever, per locked architecture constraint).
    7. Extend .gitignore (preserve any uv-generated entries) with: `.venv/`, `__pycache__/`, `*.pyc`, `.pytest_cache/`, `.ruff_cache/`, `artifacts/`, and the corpus pattern exactly as three lines: `tests/corpus/*`, `!tests/corpus/MANIFEST.md`, `!tests/corpus/manifest.json` (D-02; manifest.json is the machine-readable companion plan 01-02 writes and plan 01-06's integration tests consume).
    8. Write README.md: project name, one-paragraph description (federal RFP → compliance matrix; Phase 1 = parsing/structure library + CLI), stack note (Python 3.12, uv, pdfplumber, python-docx, Pydantic), a "secrets via env vars only" note, and a "Status: Phase 1 in progress" line. Plan 01-06 adds usage docs.
  </action>
  <verify>
    <automated>uv sync --locked && uv run rfp-analyzer --help && uv run ruff check . && git check-ignore -q tests/corpus/somefile.pdf</automated>
  </verify>
  <done>uv-managed project syncs from lockfile; entry point resolves; ruff passes; corpus paths are git-ignored.</done>
  <acceptance_criteria>
    - `uv run python -c "import pdfplumber, docx, pydantic"` exits 0
    - pyproject.toml contains `rfp-analyzer = "rfp_analyzer.cli:main"` and `requires-python = ">=3.12"`
    - `git check-ignore tests/corpus/x.pdf` exits 0 AND `git check-ignore tests/corpus/MANIFEST.md` exits 1 (MANIFEST is trackable)
    - uv.lock exists and is tracked by git (`git ls-files --error-unmatch uv.lock` after commit)
    - `grep -c "fitz\|pymupdf" pyproject.toml` returns 0 (no AGPL deps)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the document-map schema (Phase 2 contract) and RunMetrics scaffolding</name>
  <files>src/rfp_analyzer/pipeline/models.py, src/rfp_analyzer/pipeline/metrics.py, tests/unit/test_models.py</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 6 "Document map schema" — the prescriptive model set; Pattern 7 metrics bullet; "Design notes for the planner")
    - src/rfp_analyzer/pipeline/__init__.py (created in Task 1)
  </read_first>
  <behavior>
    - Test 1: DocumentMap with one PDF ParsedFile (PageSpan locator) and one DOCX ParsedFile (BlockSpan locator) survives model_dump_json → model_validate_json round-trip with all fields equal
    - Test 2: Locator union discriminates on "kind": validating `{"kind": "pages", "page_start": 3, "page_end": 9}` yields PageSpan; `{"kind": "blocks", "block_start": 0, "block_end": 41}` yields BlockSpan
    - Test 3: DocumentMap() defaults include schema_version == "1.0" and a RunMetrics with llm_calls == 0, input_tokens == 0, output_tokens == 0, estimated_cost_usd == 0.0
    - Test 4: PageInfo rejects quality values outside {"ok", "scanned", "empty", "low_text", "gibberish", "pending"} (ValidationError)
    - Test 5: SectionNode supports recursive children (an "L" node containing an "L.4" child containing "L.4.2") and serializes the nesting
  </behavior>
  <action>
    Implement `models.py` following RESEARCH Pattern 6 verbatim as the target schema, Pydantic 2.13 idioms only (model_dump_json / model_validate_json, no v1 API). Models and exact field names: PageSpan (kind literal "pages", page_start, page_end — 1-indexed inclusive), BlockSpan (kind literal "blocks", block_start, block_end — 0-indexed iter_inner_content ordinals), `Locator = PageSpan | BlockSpan` discriminated by "kind"; PageInfo (page_number, quality, char_count, metrics dict, text); BlockInfo (ordinal, kind literal "paragraph"|"table", text, style, table cells) — typed model, not raw dict, improving on the sketch's `blocks: list[dict]`; SectionNode (label, title, role, locator, detection, children — recursive); ParsedFile (file_id, filename, sha256, file_type, parse_status literal "ok"|"failed"|"rejected", error, doc_role literal "base_solicitation"|"amendment"|"attachment"|"unknown", amendment_number, amendment_evidence, page_count, pages, blocks, stripped_headers, sections, plus `first_page_layout_text: str | None = None` — layout-mode page-1 text captured by the PDF parser for SF-form field extraction, per RESEARCH Open Question 3); DocumentMap (schema_version default "1.0", package_name, classification literal "full_ucf"|"partial_ucf"|"non_ucf_commercial"|"unknown", classification_evidence, warnings, files, metrics).
    Extend PageInfo.quality literal with "pending" as the pre-quality-stage default so parsing can construct PageInfo before the quality stage runs (document this in a docstring: serialized maps must never contain "pending"; the quality stage assigns final status).
    Implement `metrics.py` with RunMetrics per D-09: stage_timings dict[str, float], files_parsed int, pages_parsed int, pages_flagged int, llm_calls int = 0, input_tokens int = 0, output_tokens int = 0, estimated_cost_usd float = 0.0. DocumentMap.metrics is typed RunMetrics with a default_factory.
    Write tests/unit/test_models.py implementing the five behaviors above; run them RED first if practical, then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_models.py -x -q</automated>
  </verify>
  <done>All five behavior tests pass; models importable as `from rfp_analyzer.pipeline.models import DocumentMap`.</done>
  <acceptance_criteria>
    - `uv run python -c "from rfp_analyzer.pipeline.models import DocumentMap, ParsedFile, PageInfo, BlockInfo, SectionNode, PageSpan, BlockSpan"` exits 0
    - `uv run python -c "from rfp_analyzer.pipeline.metrics import RunMetrics; assert RunMetrics().llm_calls == 0"` exits 0
    - `grep -c "model_dump_json\|model_validate_json" tests/unit/test_models.py` >= 1 (round-trip actually tested)
    - `grep -c "parse_raw\|\.dict()" src/rfp_analyzer/pipeline/models.py` returns 0 (no Pydantic v1 idioms)
    - pytest exit code 0 with >= 5 tests collected in test_models.py
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rename branch to main, create the public GitHub repo, add CI, push, confirm green</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-parsing-structure-foundation/01-RESEARCH.md (Pattern 7 CI YAML — verified against uv official docs; "Environment Availability" — gh authenticated as rconn0925, repo has no remote)
    - .gitignore (confirm corpus exclusion is committed BEFORE the repo goes public)
  </read_first>
  <action>
    1. Rename the default branch: `git branch -m master main`.
    2. Create `.github/workflows/ci.yml` per RESEARCH Pattern 7: name `ci`, trigger `on: [push, pull_request]`, single job on ubuntu-latest with steps: actions/checkout@v4, astral-sh/setup-uv@v8 with python-version "3.12", `uv sync --locked --dev`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run pytest tests/unit`. CI runs tests/unit only — corpus integration tests (plan 01-06) auto-skip without corpus binaries, which never exist in CI.
    3. Run `uv run ruff format .` once so the format-check step passes, commit all Task 1–3 artifacts (conventional commits, e.g. `feat(01-01): scaffold uv project with document-map schema and CI`).
    4. Create the public repo and push: `gh repo create rfp-analyzer --public --source . --remote origin --push` (gh is authenticated as rconn0925 per D-07). Note: commit_docs is true, so `.planning/` history is committed and public — acceptable and intended for a portfolio repo; do NOT add .planning to .gitignore.
    5. Confirm CI: `gh run watch` (or poll `gh run list --limit 1`) until the run completes; if it fails, fix and push until green.
  </action>
  <verify>
    <automated>gh repo view rconn0925/rfp-analyzer --json visibility,defaultBranchRef -q '.visibility + " " + .defaultBranchRef.name' && gh run list --limit 1 --json conclusion -q '.[0].conclusion'</automated>
  </verify>
  <done>Public repo exists with default branch main; latest CI run conclusion is "success".</done>
  <acceptance_criteria>
    - `gh repo view rconn0925/rfp-analyzer --json visibility -q .visibility` outputs PUBLIC
    - `git remote get-url origin` outputs a github.com/rconn0925/rfp-analyzer URL
    - `git branch --show-current` outputs main
    - `gh run list --limit 1 --json conclusion -q '.[0].conclusion'` outputs success
    - `git ls-files tests/corpus/` outputs nothing or only manifest files (no binaries tracked)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local env | Third-party packages installed into the dev environment |
| local repo → public GitHub | Everything committed becomes world-readable at push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | uv add pdfplumber/python-docx/pydantic/pytest/pytest-cov/ruff | mitigate | slopcheck audit already run in research: 6/6 [OK], zero [ASSUMED]/[SUS] — install pinned versions from RESEARCH Standard Stack only; uv.lock committed pins hashes |
| T-01-01 | Information Disclosure | git push to public repo | mitigate | .gitignore corpus + artifacts patterns committed before repo creation (Task 1 precedes Task 3); no secrets exist in Phase 1; env-var-only policy stated in README |
| T-01-02 | Tampering | .github/workflows/ci.yml third-party actions | mitigate | Only official actions: actions/checkout@v4 and astral-sh/setup-uv@v8 (verified in uv docs); no third-party marketplace actions |
</threat_model>

<verification>
- `uv sync --locked && uv run pytest tests/unit -q` passes locally
- `gh run list --limit 1` shows a green CI run on the public repo
- `git check-ignore tests/corpus/anything.pdf` exits 0
</verification>

<success_criteria>
- Public GitHub repo rconn0925/rfp-analyzer on branch main with green CI (lint + format + unit tests)
- `from rfp_analyzer.pipeline.models import DocumentMap` works; schema matches RESEARCH Pattern 6 with the Locator union and RunMetrics scaffolding
- Corpus paths git-ignored before any corpus file exists
</success_criteria>

<output>
Create `.planning/phases/01-parsing-structure-foundation/01-01-SUMMARY.md` when done
</output>
