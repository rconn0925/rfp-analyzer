---
phase: 02-requirement-extraction-grounding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/rfp_analyzer/pipeline/grounding/verify.py
  - tests/unit/test_grounding.py
autonomous: false
requirements: [EXTR-02]

must_haves:
  truths:
    - "A verbatim quote that exists on a page (modulo ligatures/hyphenation/whitespace) grounds to that page with match='exact' or 'fuzzy' and verified=True"
    - "A hallucinated quote that is not locatable on any page in its chunk yields verified=False, match='none', page=None — it is flagged, never dropped, never given an invented page"
    - "The page number in a SourceRef is computed from the chunk page_map, never taken from model output"
    - "Every SourceRef carries the chunk's doc_role (base_solicitation/amendment/attachment/unknown) — on both grounded and ungroundable rows — so amendment gating downstream is traceable"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/grounding/verify.py"
      provides: "ground() locate primitive + build_source_ref() over a Chunk"
      exports: ["ground", "build_source_ref"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/grounding/verify.py"
      to: "src/rfp_analyzer/pipeline/grounding/normalize.py"
      via: "normalizes both quote and page text before matching"
      pattern: "from rfp_analyzer.pipeline.grounding.normalize import normalize"
    - from: "src/rfp_analyzer/pipeline/grounding/verify.py"
      to: "rapidfuzz.fuzz"
      via: "partial_ratio_alignment for fuzzy span location"
      pattern: "partial_ratio_alignment"
---

<objective>
Build the honesty backbone (EXTR-02): verify every `verbatim_text` against real
page text and compute a `SourceRef` from the document map — never from a
model-emitted citation. This is also the prompt-injection / hallucination
backstop: any fabricated "requirement" that is not verbatim-locatable on a real
page is flagged `verified=False`.

Purpose: No LLM-generated citation may reach output; unverifiable quotes are
surfaced, not silently dropped.
Output: `grounding/verify.py`; `rapidfuzz` dependency; unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/pipeline/grounding/normalize.py

<interfaces>
<!-- From 02-01 (models.py). -->
SourceRef(file_id, filename, section_label:str|None, page:int|None,
  char_start:int|None, char_end:int|None, match:Literal["exact","fuzzy","none"],
  score:float, verified:bool, doc_role:str)   # doc_role copied from the Chunk
Chunk(file_id, filename, section_label, role, doc_role, text,
  page_map:list[tuple[int,int,int]])   # (char_start, char_end, page_number) over chunk.text

<!-- rapidfuzz 3.14.5 (verified installed in RESEARCH): -->
from rapidfuzz import fuzz
fuzz.partial_ratio_alignment(query, haystack) -> object with .score, .src_start, .src_end
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — rapidfuzz</name>
  <files>pyproject.toml</files>
  <action>BLOCKING legitimacy gate before any `uv add rapidfuzz`. rapidfuzz is tagged [ASSUMED] in the RESEARCH Package Legitimacy Audit (discovered via training/WebSearch, not authoritative docs; slopcheck-clean but provenance-assumed), so per the package-legitimacy protocol it requires human verification and is never auto-approvable. Present the verification facts to the human and wait for explicit approval; do not install until approved.</action>
  <what-built>Nothing yet — this gate precedes installing `rapidfuzz`, tagged [ASSUMED] in the RESEARCH Package Legitimacy Audit (discovered via training/WebSearch, not authoritative docs; slopcheck-clean but provenance-assumed).</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/rapidfuzz/ — confirm it exists, is actively maintained, links to github.com/rapidfuzz/RapidFuzz, and version 3.14.5 is published.
    2. Confirm it is the intended MIT-licensed C++-backed fuzzy-matching library (not a typosquat).
  </how-to-verify>
  <verify><human-check>Human confirms rapidfuzz on pypi.org/project/rapidfuzz is the legitimate RapidFuzz package (real repo, active, 3.14.5 published) before install.</human-check></verify>
  <done>Human approved installing rapidfuzz (or named an alternative); install proceeds only after approval.</done>
  <resume-signal>Type "approved" to proceed with `uv add rapidfuzz`, or name an alternative.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ground() — normalize, locate, score</name>
  <files>pyproject.toml, uv.lock, src/rfp_analyzer/pipeline/grounding/verify.py, tests/unit/test_grounding.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 5 + Pitfall 5), src/rfp_analyzer/pipeline/grounding/normalize.py</read_first>
  <behavior>
    - Exact substring (after normalize) → returns ("exact", start, end, 100.0).
    - Only-differs-by-ligature/hyphenation quote → still grounds exact (because both sides normalized).
    - Noisy-but-close quote (residual OCR whitespace) with partial_ratio_alignment score ≥ threshold → ("fuzzy", src_start, src_end, score).
    - Absent quote (score below threshold) → None.
    - Empty or whitespace-only quote → None (never spuriously grounds).
  </behavior>
  <action>Run `uv add rapidfuzz` (approved in Task 1). Create verify.py exporting `ground(verbatim: str, page_text: str, threshold: float = 92.0)` implementing the RESEARCH Pattern 5 recipe: normalize both sides, try `hay.find(q)` for an exact hit (score 100.0), else `fuzz.partial_ratio_alignment(q, hay)` and accept when `.score >= threshold`, returning a `(match_kind, start, end, score)` tuple or None. Guard empty/whitespace query → None. Make `threshold` a module-level default constant so the eval can tune it later.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_grounding.py -q` passes the exact, ligature, fuzzy, absent, and empty cases.
    - `grep -q "partial_ratio_alignment" src/rfp_analyzer/pipeline/grounding/verify.py`.
    - `grep -q "rapidfuzz" pyproject.toml`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_grounding.py -q</automated></verify>
  <done>ground() locates real quotes (exact then fuzzy), rejects absent/empty ones, and returns a scored span.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: build_source_ref() — map matched offset to a real page</name>
  <files>src/rfp_analyzer/pipeline/grounding/verify.py, tests/unit/test_grounding.py</files>
  <read_first>src/rfp_analyzer/pipeline/grounding/verify.py (Task 2 output), .planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 5 "Locate" + Open Question 2 — page is the load-bearing output, char offsets best-effort)</read_first>
  <behavior>
    - Given a Chunk whose page_map splits chunk.text across pages 49 and 50, a quote living in the page-50 region returns SourceRef.page == 50 (computed from page_map, not passed in).
    - A groundable quote → SourceRef.verified=True, match in {"exact","fuzzy"}, file_id/filename/section_label copied from the Chunk.
    - An ungroundable quote → SourceRef.verified=False, match="none", page=None, score below threshold — the requirement is retained and flagged, per the "never drop silently" rule.
    - EVERY returned SourceRef carries doc_role == chunk.doc_role, on both the grounded and the ungroundable path (an ungroundable amendment quote must still know it came from an amendment, so INTK-03 gating stays correct).
    - The function NEVER accepts a caller-supplied page number for the ref.
  </behavior>
  <action>Add `build_source_ref(verbatim: str, chunk: Chunk, threshold: float = 92.0) -> SourceRef` to verify.py. Ground the verbatim against the chunk's normalized text; on a hit, translate the matched char offset into a page_number by scanning chunk.page_map for the (char_start, char_end) band containing the match start; build SourceRef with that page, char_start/char_end best-effort, match kind, score, verified=True, and file_id/filename/section_label/doc_role from the chunk (doc_role=chunk.doc_role). On a miss, return SourceRef(verified=False, match="none", page=None, char_start=None, char_end=None, score=&lt;score&gt;) still carrying file_id/filename/section_label AND doc_role=chunk.doc_role so the flag is traceable and amendment gating downstream still works. Import SourceRef and Chunk from pipeline.models.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_grounding.py -q` passes including a multi-page page_map test asserting the correct page is chosen and an ungroundable-quote test asserting verified=False / page=None.
    - A test asserts both a grounded and an ungroundable SourceRef built from a Chunk with doc_role="amendment" carry doc_role=="amendment" (doc_role is threaded on both paths).
    - Source assertion: verify.py contains no parameter or code path that copies a page number from anything other than chunk.page_map (reviewer-checkable; add a test that a Chunk with page_map pages [49,50] can only ever yield page in {49,50} or None).
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_grounding.py -q</automated></verify>
  <done>build_source_ref computes verified page refs from the map, threads doc_role onto every ref (grounded and ungroundable), and flags ungroundable quotes with verified=False and page=None — the EXTR-02 honesty backbone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → grounding | LLM-produced verbatim_text is untrusted; grounding is the gate that admits only page-locatable spans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing | Hallucinated requirement with fabricated provenance | mitigate | build_source_ref grounds every verbatim against real page text; unlocatable → verified=False/page=None (Task 3) — the core anti-hallucination control |
| T-02-04 | Tampering | Prompt-injected "requirement" ("ignore instructions…") smuggled from document text | mitigate | Same grounding gate: injected text that is not a real contiguous page span is flagged, not emitted as verified |
| T-02-SC | Tampering | `rapidfuzz` install (pip supply chain) | mitigate | RESEARCH slopcheck clean; blocking-human legitimacy gate on pypi.org before install (Task 1) — [ASSUMED] package, never auto-approved |
</threat_model>

<verification>
- `uv run python -m pytest tests/unit/test_grounding.py -q` passes; runs pure-Python in CI (rapidfuzz is a normal install, no Ollama/GPU).
- `uv run ruff check src/rfp_analyzer/pipeline/grounding/verify.py` clean.
- `uv lock --check` (or equivalent) confirms rapidfuzz pinned.
</verification>

<success_criteria>
Every verbatim can be verified against real page text; verified references carry a
map-computed page; every ref carries chunk doc_role provenance; unverifiable quotes are
flagged verified=False and never assigned an invented page — satisfying EXTR-02.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
