---
phase: 02-requirement-extraction-grounding
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/eval/__init__.py
  - tests/eval/golden/golden_set.json
  - tests/eval/golden/review-note.md
  - tests/eval/golden/schema.md
autonomous: true
requirements: [EXTR-01, EXTR-05]

must_haves:
  truths:
    - "A hand-shredded ground-truth requirement set for the primary package N4008526R0033 exists in-repo, each entry carrying file_id, page, verbatim_text, atomic_obligation, binding_keyword, and req_type"
    - "The golden set was drafted by one pass and adversarially validated by a second independent pass that found misses/over-splits/wrong-page/wrong-verbatim and reconciled them"
    - "A companion review-note.md records how it was built, the validator's findings, and the honest caveat that an agent-built baseline shares blind spots with the extractor (mitigated by the EXTR-05 sweep)"
    - "Every golden verbatim_text is a real contiguous span locatable on its cited page of the source PDF"
  artifacts:
    - path: "tests/eval/golden/golden_set.json"
      provides: "ground-truth requirements for recall/precision scoring"
      min_lines: 40
      contains: "verbatim_text"
    - path: "tests/eval/golden/review-note.md"
      provides: "build method, adversarial findings, honest caveat, Ross spot-check pointer"
  key_links:
    - from: "tests/eval/golden/golden_set.json"
      to: "tests/corpus/primary-ucf (N4008526R0033) document map"
      via: "each entry's page/verbatim verified against the source PDF text layer"
      pattern: "N4008526R0033"
---

<objective>
Construct the measurement baseline (success criterion 5): a hand-shredded,
adversarially-validated golden-set of ground-truth requirements for the primary
package N4008526R0033. Two independent passes — draft, then adversarial
validation — replace the human checkpoint per the locked autonomous decision.
Persisted in-repo as text (not a binary) with a companion review note.

Purpose: Without a trustworthy golden set, recall/precision are unmeasurable and
the model bake-off is meaningless.
Output: `tests/eval/golden/golden_set.json`, `review-note.md`, `schema.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-extraction-grounding/02-CONTEXT.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@tests/corpus/manifest.json
@.planning/phases/01-parsing-structure-foundation/01-06-SUMMARY.md

<interfaces>
<!-- Golden entry shape must be scorable against extraction output (see 02-07 eval matcher):
     each golden entry needs at minimum file_id, page (1-indexed), verbatim_text,
     atomic_obligation, binding_keyword, req_type. Mirror the Requirement fields from
     02-01 models.py so the eval matcher compares like-for-like. -->
Requirement(verbatim_text, atomic_obligation, binding_keyword, req_type, source_ref{file_id,page})
Primary package section boundaries (verified in Phase 1): L @ pages 49-57, M @ pages 58-70,
C @ pages 10-11 of "Solicitation - N4008526R0033.pdf"; plus "Section C Annexes" (161pp) and
"SECTION F ANNEX" (18pp) attachments carry requirements outside L/M (EXTR-04 coverage).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the ground-truth golden set (pass A)</name>
  <files>tests/eval/__init__.py, tests/eval/golden/schema.md, tests/eval/golden/golden_set.json</files>
  <read_first>tests/corpus/manifest.json (primary-ucf files + verified L/M page starts), src/rfp_analyzer/pipeline/models.py (Requirement field names to mirror), .planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 3 atomic splitting rule — define the golden set at the SAME atomic granularity the extractor targets)</read_first>
  <action>Generate the primary package's document map first: `uv run rfp-analyzer parse tests/corpus/primary-ucf --out artifacts` (or load an existing artifact) to get section text with page numbers. Then hand-shred the requirement-bearing sections — Section L (p49-57), Section M (p58-70), Section C (p10-11), and a representative sample from the Section C Annexes and SECTION F ANNEX attachments (to exercise EXTR-04) — into ground-truth requirements. For each, record: file_id, page, verbatim_text (a REAL contiguous span copied from the source page text, not paraphrased), atomic_obligation (single-obligation rewrite following the RESEARCH Pattern 3 splitting rule: split coordinated enumerations and coordinated verb phrases; do not split conditionals), binding_keyword, and req_type. Write schema.md documenting the entry shape. Persist as pretty-printed JSON. Scope: aim for solid coverage of L/M plus enough C/attachment entries to make EXTR-04 recall measurable — completeness over the whole 290-page package is not required for v1, but record what was and was not covered in the review note (Task 2).</action>
  <acceptance_criteria>
    - `tests/eval/golden/golden_set.json` exists, is valid JSON (`uv run python -c "import json; json.load(open('tests/eval/golden/golden_set.json'))"` exits 0), and has ≥30 entries spanning L, M, and at least one attachment/C source.
    - Every entry has non-empty file_id, integer page, non-empty verbatim_text, atomic_obligation, binding_keyword, req_type.
    - `grep -q "verbatim_text" tests/eval/golden/schema.md`.
  </acceptance_criteria>
  <verify><automated>uv run python -c "import json; d=json.load(open('tests/eval/golden/golden_set.json')); assert len(d)>=30 or len(d.get('requirements',[]))>=30"</automated></verify>
  <done>A first-pass ground-truth requirement set for N4008526R0033 exists in-repo with verbatim spans and page refs.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial validation + reconciliation (pass B)</name>
  <files>tests/eval/golden/golden_set.json, tests/eval/golden/review-note.md</files>
  <read_first>tests/eval/golden/golden_set.json (pass A output), tests/corpus/primary-ucf source PDFs (via a fresh parse/read), .planning/phases/02-requirement-extraction-grounding/02-CONTEXT.md (the two-pass adversarial decision + honest caveat)</read_first>
  <action>Perform an INDEPENDENT adversarial validation of the pass-A golden set against the source PDF text: (a) find MISSES — binding requirements on the covered pages that pass A omitted; (b) find OVER-SPLITS / under-splits against the Pattern 3 atomic rule; (c) verify every verbatim_text actually appears (string-match) on its cited page and fix wrong spans; (d) verify every page number. Reconcile: add missed entries, correct spans/pages, adjust splits. Then write review-note.md recording: how the set was built (two-pass), the concrete findings of this validation pass (counts of misses/fixes), what sections/pages were and were not covered, the honest caveat that an agent-built baseline shares blind spots with the extractor (and that the EXTR-05 deterministic sweep is the independent mitigation), and a short "how Ross can spot-check this later" pointer (non-blocking).</action>
  <acceptance_criteria>
    - review-note.md exists and contains sections for build-method, adversarial-findings (with at least a miss count and a span-fix count), coverage scope, and the shared-blind-spot caveat.
    - Re-validate every verbatim: a checker script confirms each golden verbatim_text (after NFKC+whitespace normalization) is a substring of its cited page's extracted text; the review note states the pass rate and lists any deliberate fuzzy exceptions.
    - `grep -qi "caveat\|blind spot" tests/eval/golden/review-note.md`.
  </acceptance_criteria>
  <verify><automated>uv run python -c "import json; d=json.load(open('tests/eval/golden/golden_set.json')); assert (len(d) if isinstance(d,list) else len(d['requirements']))>=30"</automated></verify>
  <done>The golden set is adversarially validated and reconciled; every verbatim is locatable on its page; a review note records the method, findings, coverage, and honest caveat for Ross's optional spot-check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden set → eval | The golden set is the yardstick; if wrong, every recall/precision number is wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Repudiation | Golden set silently shares the extractor's blind spots, inflating recall | mitigate | Two independent passes (draft + adversarial validate); the EXTR-05 deterministic sweep is a model-independent cross-check; caveat recorded honestly (Task 2) |
| T-02-09 | Tampering | Fabricated golden verbatim that is not actually on the page | mitigate | Task 2 re-validates every verbatim as a real substring of its cited page's text |
</threat_model>

<verification>
- golden_set.json is valid JSON with ≥30 verified entries spanning L/M and ≥1 attachment/C source.
- review-note.md documents the adversarial pass and the honest caveat.
- This plan writes only data/docs under tests/eval/golden/ — no code, no dependency changes, CI-neutral.
</verification>

<success_criteria>
A trustworthy, adversarially-validated ground-truth golden set for N4008526R0033
exists in-repo with a companion review note — the measurable baseline that
success criterion 5 (recall/precision every run) depends on.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-04-SUMMARY.md` when done.
</output>
