---
phase: 02-requirement-extraction-grounding
plan: 06
type: execute
wave: 4
depends_on: ["02-03", "02-05"]
files_modified:
  - src/rfp_analyzer/pipeline/extraction/run_extraction.py
  - src/rfp_analyzer/cli.py
  - tests/unit/test_cli_extract.py
  - tests/integration/test_extract_corpus.py
  - tests/integration/conftest.py
autonomous: true
requirements: [EXTR-01, EXTR-04, EXTR-05, INTK-03]

must_haves:
  truths:
    - "run_extraction(document_map, model, seed) wires chunk → extract → sweep-reconcile → amendment-flag into one RequirementSet, populating RunMetrics (llm_calls, tokens, latency, model_name)"
    - "The authoritative binding_keyword from the deterministic sweep overrides the model's guess, and missed_candidates are attached to the RequirementSet (EXTR-05)"
    - "rfp-analyzer extract <artifacts-dir> emits requirements.json plus a stdout report: counts by type/section, verified/unverified counts, the missed-candidate list, amendment change rows, and the metrics footer (Phase 1 dual-output pattern)"
    - "End-to-end extraction over the primary corpus package produces requirements drawn from L, M, AND attachments/C (EXTR-04) with grounded refs"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/extraction/run_extraction.py"
      provides: "run_extraction() single pipeline entry producing RequirementSet"
      exports: ["run_extraction"]
    - path: "src/rfp_analyzer/cli.py"
      provides: "extract subcommand (dual output: requirements.json + report)"
      contains: "extract"
  key_links:
    - from: "src/rfp_analyzer/cli.py"
      to: "src/rfp_analyzer/pipeline/extraction/run_extraction.py"
      via: "extract subcommand loads document_map.json, calls run_extraction, writes requirements.json + report"
      pattern: "run_extraction"
    - from: "src/rfp_analyzer/pipeline/extraction/run_extraction.py"
      to: "sweep.reconcile + amendments.flag_amendments"
      via: "post-processes extracted requirements with the model-independent checks"
      pattern: "reconcile"
---

<objective>
Wire the pieces into one entry point and expose it on the CLI. `run_extraction`
composes chunker → extract → sweep-reconcile → amendment-flag into a
`RequirementSet` with real metrics; the `extract` subcommand emits the
`requirements.json` artifact and a practitioner-readable stdout report following
the Phase 1 dual-output pattern.

Purpose: A single CLI-verifiable command turns a document map into grounded
requirements — the phase's user-facing deliverable.
Output: `extraction/run_extraction.py`; `extract` CLI subcommand; corpus
integration test (skipif Ollama).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/cli.py
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/metrics.py
@src/rfp_analyzer/pipeline/extraction/extract.py
@src/rfp_analyzer/pipeline/sweep.py
@src/rfp_analyzer/pipeline/amendments.py
@tests/integration/conftest.py

<interfaces>
<!-- From prior plans: iter_chunks(document_map) [02-03]; extract_requirements(chunks, model, seed) [02-05];
     sweep_hits(page_text, page)/reconcile(reqs, hits)->(reqs, missed) [02-03];
     flag_amendments(reqs)->reqs [02-03]; RequirementSet(package_name, model_name, requirements,
     missed_candidates, metrics) [02-01]; DocumentMap [Phase 1].
     Existing CLI: build_parser() with a `parse` subparser; render_report(document_map)->str;
     main() dispatches on args.command. Follow the same argparse + dual-output shape. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_extraction pipeline entry (compose + metrics)</name>
  <files>src/rfp_analyzer/pipeline/extraction/run_extraction.py, tests/unit/test_cli_extract.py</files>
  <read_first>src/rfp_analyzer/pipeline/run.py (Phase 1 orchestration style: pure, timed via perf_counter into RunMetrics.stage_timings, no printing), src/rfp_analyzer/pipeline/metrics.py, src/rfp_analyzer/pipeline/extraction/extract.py, src/rfp_analyzer/pipeline/sweep.py, src/rfp_analyzer/pipeline/amendments.py, .planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (CLAUDE.md metrics-repurpose note: populate llm_calls/tokens/latency; model_name + latency via stage_timings keys, estimated_cost_usd stays 0.0)</read_first>
  <action>Create run_extraction.py exporting `run_extraction(document_map: DocumentMap, model: str = "qwen2.5:14b-instruct", seed: int = 7) -> RequirementSet`. Pure orchestration (no printing/CLI imports — same boundary as run.py): (1) chunks = list(iter_chunks(document_map)); (2) requirements = extract_requirements(chunks, model, seed) — accumulate llm_calls/input_tokens/output_tokens into a RunMetrics; (3) build sweep hits over every ok page's text across all files and call reconcile(requirements, hits) → (requirements_with_authoritative_keywords, missed_candidates); (4) requirements = flag_amendments(requirements); (5) return RequirementSet(package_name=document_map.package_name, model_name=model, requirements=..., missed_candidates=..., metrics=...). Record stage timings (extract/sweep/amendments) and model_name in metrics; keep estimated_cost_usd=0.0 (local). Time with perf_counter like run.py.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_cli_extract.py -q` passes a run_extraction test that injects a fake extract path (monkeypatch extract_requirements or pass a stub) over a small hand-built DocumentMap and asserts: RequirementSet.model_name set, missed_candidates populated when a sweep hit is uncovered, binding_keyword overridden from the sweep, and metrics.estimated_cost_usd == 0.0.
    - `grep -q "def run_extraction" src/rfp_analyzer/pipeline/extraction/run_extraction.py`; no `import argparse` / no `print(` in the module.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_cli_extract.py -q</automated></verify>
  <done>run_extraction composes chunk→extract→reconcile→amendments into a metricized RequirementSet as a pure library entry point.</done>
</task>

<task type="auto">
  <name>Task 2: extract CLI subcommand + report + artifact</name>
  <files>src/rfp_analyzer/cli.py, tests/unit/test_cli_extract.py</files>
  <read_first>src/rfp_analyzer/cli.py (full — mirror build_parser, render_report, exit-code and dual-output conventions), src/rfp_analyzer/pipeline/extraction/run_extraction.py (Task 1)</read_first>
  <action>Add an `extract` subparser to build_parser(): positional `artifacts_dir` (a directory containing a `document_map.json` from `parse`, or the package artifact dir), `--model` (default "qwen2.5:14b-instruct"), `--seed` (default 7), `--out` (default "artifacts"). Add `_run_extract(args)`: load and DocumentMap.model_validate_json the document_map.json (schema_version check — honest error if incompatible), call run_extraction, write `requirements.json` (RequirementSet.model_dump_json(indent=2)) next to the map, and print a `render_requirements_report(requirement_set)` to stdout showing: counts by req_type and by section, verified vs unverified (verified=False) counts, the missed_candidates list (EXTR-05 surfaced gaps), amendment change rows (is_amendment_change / possibly_modified — INTK-03), and a metrics footer (model, llm_calls, tokens, latency; LLM cost $0.00 local). Wire `main()` to dispatch `extract`. Reuse the Windows console reconfigure. Exit codes consistent with Phase 1 (0 success; 2 usage/missing map).</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_cli_extract.py -q` passes: a report-rendering test over a hand-built RequirementSet asserts the report contains a type/section breakdown, an unverified count, a "missed" section, and the metrics footer; a missing-document_map.json invocation returns exit 2.
    - `grep -q '"extract"' src/rfp_analyzer/cli.py` (subparser registered) and `grep -q "requirements.json" src/rfp_analyzer/cli.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_cli_extract.py -q</automated></verify>
  <done>`rfp-analyzer extract` produces requirements.json + a practitioner report surfacing types, unverified flags, missed candidates, amendment rows, and metrics.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end corpus integration test (skipif Ollama)</name>
  <files>tests/integration/test_extract_corpus.py, tests/integration/conftest.py</files>
  <read_first>tests/integration/conftest.py (corpus-skip + session pipeline_map fixture to mirror), tests/corpus/manifest.json (primary-ucf expectations), src/rfp_analyzer/pipeline/extraction/run_extraction.py</read_first>
  <action>Add an `ollama_available()` guard to conftest.py (a cheap `GET http://localhost:11434/api/version` via httpx/urllib with a short timeout, returning False on any error) and a collection/skip helper so extraction integration tests SKIP when Ollama is unreachable OR the corpus is absent — mirroring the existing corpus-skip pattern (CI has neither GPU nor corpus). Create test_extract_corpus.py: run run_extraction over the primary-ucf DocumentMap (reuse the session pipeline_map fixture) with qwen2.5:14b-instruct and assert phase truths: (a) requirements come from ≥2 distinct section_labels including at least one attachment/C source and Sections L and M (EXTR-04); (b) every requirement with verified=True has a page within its cited file; (c) missed_candidates is a list (EXTR-05 wired); (d) at least the amendment files contribute rows and no base+amendment merge occurred (INTK-03). Keep assertions tolerant of small model variance (Pitfall 6) — assert structural truths, not exact counts.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/integration/test_extract_corpus.py -q` PASSES locally with Ollama+corpus present, and SKIPS cleanly (0 failures) when either is absent — verify the skip by temporarily unsetting one condition.
    - `grep -q "ollama_available\|api/version" tests/integration/conftest.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/integration/test_extract_corpus.py -q</automated></verify>
  <done>An end-to-end extraction over the real primary package proves L/M + attachment/C coverage, grounded refs, wired sweep, and no-merge amendments — and auto-skips in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document_map.json → CLI | Loaded artifact is schema-validated (Pydantic) before use; incompatible schema_version fails honestly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering | Malformed/tampered document_map.json crashes or mis-drives extraction | mitigate | DocumentMap.model_validate_json + schema_version check with an honest error (Task 2) |
| T-02-15 | Info Disclosure | Unverified/hallucinated requirements silently blend into output | mitigate | Report separates verified vs unverified counts and lists missed_candidates so gaps are visible, not hidden (Task 2) |
| T-02-16 | Denial of Service | A huge package makes extraction run unbounded | accept | Local CLI; slow runs are visible; hard timeouts revisited before Phase 4 web upload (RESEARCH Security Domain) |
</threat_model>

<verification>
- `uv run python -m pytest tests/unit/test_cli_extract.py -q` passes in CI (pure, injected fakes).
- `tests/integration/test_extract_corpus.py` passes locally with Ollama+corpus; skips in CI.
- `uv run ruff check src/rfp_analyzer/cli.py src/rfp_analyzer/pipeline/extraction/run_extraction.py` clean.
- Local: `uv run rfp-analyzer extract artifacts/primary-ucf --model qwen2.5:14b-instruct` writes requirements.json + prints the report (evidence captured in SUMMARY).
</verification>

<success_criteria>
One CLI command turns a document map into a grounded RequirementSet artifact +
report drawing requirements from L, M, and attachments/C (EXTR-01/04), surfacing
missed candidates (EXTR-05) and amendment change rows (INTK-03), with real local
metrics.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-06-SUMMARY.md` when done.
</output>
