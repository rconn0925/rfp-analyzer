---
phase: 02-requirement-extraction-grounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rfp_analyzer/pipeline/models.py
  - src/rfp_analyzer/pipeline/grounding/__init__.py
  - src/rfp_analyzer/pipeline/grounding/normalize.py
  - src/rfp_analyzer/pipeline/ids.py
  - tests/unit/test_requirement_models.py
  - tests/unit/test_normalize.py
  - tests/unit/test_ids.py
autonomous: true
requirements: [EXTR-01, EXTR-02]

must_haves:
  truths:
    - "The Requirement schema carries verbatim_text, atomic_obligation, a stable requirement_id, req_type, binding_keyword, and a SourceRef — the full EXTR-01/02/03 contract"
    - "The SourceRef carries doc_role (base_solicitation/amendment/attachment/unknown) so amendment gating (INTK-03) can tell amendment rows from base rows without re-reading the chunk"
    - "Re-running ID assignment over identical inputs yields identical requirement_ids (order-independent, content-derived)"
    - "The shared normalizer folds NFKC ligatures, de-hyphenates soft line breaks, and collapses whitespace so a quote and its page text compare equal"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/models.py"
      provides: "SourceRef, RequirementDraft, RequirementBatch, Requirement, Chunk, MissedCandidate, RequirementSet Pydantic models"
      contains: "class Requirement"
    - path: "src/rfp_analyzer/pipeline/grounding/normalize.py"
      provides: "normalize(text) shared by grounding, ids, sweep"
      exports: ["normalize"]
    - path: "src/rfp_analyzer/pipeline/ids.py"
      provides: "content-hash stable requirement_id + display_label"
      exports: ["requirement_id", "display_label"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/ids.py"
      to: "src/rfp_analyzer/pipeline/grounding/normalize.py"
      via: "imports normalize so ID keys use the same canonical form grounding uses"
      pattern: "from rfp_analyzer.pipeline.grounding.normalize import normalize"
---

<objective>
Establish the Phase 2 data contract and the two pure primitives every downstream
module depends on: the shared text normalizer and the content-derived stable ID
scheme. This is the interface-first foundation — grounding, chunker, sweep,
amendments, extraction, and the eval harness all import these types and functions.

Purpose: Downstream executors receive concrete contracts (types, signatures) in
their plans and never explore the codebase to discover them.
Output: Extended `models.py`; `grounding/normalize.py`; `ids.py`; unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/metrics.py

<interfaces>
<!-- Existing Phase 1 contract this plan EXTENDS. Do not break these. -->
From src/rfp_analyzer/pipeline/models.py: PageInfo(page_number, quality, text),
SectionNode(label, title, role, locator), ParsedFile(file_id, filename, sha256,
doc_role, pages, sections), DocumentMap(schema_version, package_name, files,
metrics), Locator = PageSpan(page_start,page_end) | BlockSpan(block_start,block_end).
ParsedFile.doc_role is Literal["base_solicitation","amendment","attachment","unknown"].
RunMetrics already exposes llm_calls, input_tokens, output_tokens, estimated_cost_usd.

<!-- NEW models this plan must add (RESEARCH Patterns 2,3,4). These are the
     contracts every later plan references — keep field names EXACT. -->
SourceRef(file_id:str, filename:str, section_label:str|None, page:int|None,
  char_start:int|None, char_end:int|None, match:Literal["exact","fuzzy","none"],
  score:float, verified:bool, doc_role:str)
  # doc_role carries the source ParsedFile.doc_role
  # (base_solicitation/amendment/attachment/unknown) — provenance for INTK-03
  # amendment gating; make it explicit on the ref rather than an is_amendment bool
  # so provenance stays reusable and self-describing.
RequirementDraft(verbatim_text:str, atomic_obligation:str,
  binding_keyword:Literal["shall","must","will","should","shall not","none"],
  type_guess:Literal["instruction","evaluation","sow_pws","special_requirements",
  "clause","attachment","other"], parent_index:int|None=None)
RequirementBatch(requirements:list[RequirementDraft])
Chunk(file_id:str, filename:str, section_label:str, role:str|None, doc_role:str,
  text:str, page_map:list[tuple[int,int,int]])   # (char_start,char_end,page_number)
Requirement(requirement_id:str, display_label:str, verbatim_text:str,
  atomic_obligation:str, binding_keyword:str, req_type:str, source_ref:SourceRef,
  parent_id:str|None=None, is_amendment_change:bool=False,
  possibly_modified:bool=False, affects:list[str]=[], verified:bool)
MissedCandidate(file_id:str, page:int, verbatim_sentence:str, binding_keyword:str)
RequirementSet(schema_version:str="1.0", package_name:str="", model_name:str="",
  requirements:list[Requirement]=[], missed_candidates:list[MissedCandidate]=[],
  metrics:RunMetrics=...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 2 requirement schema to models.py</name>
  <files>src/rfp_analyzer/pipeline/models.py, tests/unit/test_requirement_models.py</files>
  <read_first>src/rfp_analyzer/pipeline/models.py (full — match style, Literal usage, docstrings; note ParsedFile.doc_role is Literal["base_solicitation","amendment","attachment","unknown"]), src/rfp_analyzer/pipeline/metrics.py, .planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Patterns 2,3,4 and the "structured outputs" constraint note in CLAUDE.md)</read_first>
  <behavior>
    - RequirementBatch.model_json_schema() emits a schema with NO minimum/maximum/minLength keywords and NO recursive $ref cycles (Ollama structured-output constraint) — assert these absent in the generated schema.
    - A Requirement round-trips through model_dump_json → model_validate_json unchanged.
    - RequirementSet defaults: empty requirements, empty missed_candidates, RunMetrics() default, schema_version "1.0".
    - SourceRef with verified=False and page=None validates (an unverifiable reference is representable, never forced to invent a page).
    - SourceRef carries doc_role (a str holding the ParsedFile.doc_role value: base_solicitation/amendment/attachment/unknown) and round-trips with doc_role preserved — this is the provenance INTK-03 amendment gating keys off.
  </behavior>
  <action>Extend models.py with the exact models in the &lt;interfaces&gt; block: SourceRef, RequirementDraft, RequirementBatch, Chunk, Requirement, MissedCandidate, RequirementSet. SourceRef additionally carries `doc_role: str` (the source ParsedFile.doc_role value threaded through the chunk) so downstream amendment gating (INTK-03) can distinguish amendment rows from base rows — chosen over an is_amendment bool to keep provenance explicit and reusable. Use the same Pydantic-v2 style already in the file (BaseModel, Literal unions, Field(default_factory=...) for mutable defaults — never a bare mutable default). RequirementDraft and RequirementBatch are ALSO the Ollama `format` schema, so keep them flat: no constrained numerics, no recursive nesting, Literal enums only. Give each new class a one-line docstring matching the file's voice. Do NOT bump DocumentMap.schema_version (this adds sibling models, not a breaking change to the existing map). Reuse RunMetrics from metrics.py for RequirementSet.metrics.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_requirement_models.py -q` passes.
    - Source assertion: `grep -q "class Requirement(" src/rfp_analyzer/pipeline/models.py` and same for SourceRef, RequirementDraft, RequirementBatch, Chunk, MissedCandidate, RequirementSet.
    - `grep -q "doc_role" src/rfp_analyzer/pipeline/models.py` in the SourceRef definition, and a test asserts a SourceRef round-trips with doc_role="amendment" preserved.
    - A test asserts `"minimum" not in json.dumps(RequirementBatch.model_json_schema())` and `"minLength" not in ...` (structured-output safety).
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_requirement_models.py -q</automated></verify>
  <done>All seven new models exist, round-trip, SourceRef carries a doc_role provenance field, and RequirementBatch's JSON schema is free of the Ollama-forbidden keywords.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared text normalizer (grounding/normalize.py)</name>
  <files>src/rfp_analyzer/pipeline/grounding/__init__.py, src/rfp_analyzer/pipeline/grounding/normalize.py, tests/unit/test_normalize.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md ("Shared normalizer" code example + Pitfall 5), src/rfp_analyzer/pipeline/parsing/__init__.py (see existing sanitize_text for style parity)</read_first>
  <behavior>
    - `normalize("require-\nments")` == `"requirements"` (soft line-break de-hyphenation, including the U+00AD soft hyphen variant).
    - `normalize("ﬁle")` == `"file"` (NFKC ligature fold).
    - Runs of spaces/newlines/tabs collapse to a single space; result is stripped.
    - Idempotent: `normalize(normalize(x)) == normalize(x)`.
  </behavior>
  <action>Create the `grounding` package (`__init__.py` with a module docstring) and `normalize.py` exporting `normalize(text: str) -> str` implementing exactly the RESEARCH recipe: `unicodedata.normalize("NFKC", text)`, then `re.sub(r"(\w)[-\xad]\s*\n\s*(\w)", r"\1\2", text)` to heal wrapped hyphens, then `re.sub(r"\s+", " ", text).strip()`. Pure stdlib only. This is the single canonical normalizer — grounding, ids, and sweep all import it; do not fork variants.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_normalize.py -q` passes including the ligature, de-hyphenation, whitespace, and idempotency cases.
    - `grep -q "def normalize" src/rfp_analyzer/pipeline/grounding/normalize.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_normalize.py -q</automated></verify>
  <done>normalize() folds ligatures, de-hyphenates, collapses whitespace, is idempotent, and is importable as the shared primitive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Content-derived stable requirement IDs (ids.py)</name>
  <files>src/rfp_analyzer/pipeline/ids.py, tests/unit/test_ids.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 4), src/rfp_analyzer/pipeline/grounding/normalize.py (Task 2 output — ids MUST reuse normalize)</read_first>
  <behavior>
    - `requirement_id(file_id, verbatim, occurrence, atomic_ord)` is deterministic: identical args → identical `REQ-<10hex>` string; asserted stable across two calls.
    - Two atomic siblings sharing one verbatim span but different atomic_ord get DIFFERENT ids.
    - The same sentence appearing twice in a file (different occurrence) gets DIFFERENT ids.
    - Whitespace/ligature variants of the same verbatim (that normalize equal) produce the SAME id (id keys off normalize(verbatim), not raw).
    - `display_label(section_label, ordinal)` returns a human label like "L-3" (renumberable; NOT the stable key).
  </behavior>
  <action>Create ids.py exporting `requirement_id(file_id: str, verbatim: str, occurrence: int, atomic_ord: int) -> str` and `display_label(section_label: str, ordinal: int) -> str`. requirement_id computes `hashlib.sha256(f"{file_id}|{normalize(verbatim)}|{occurrence}|{atomic_ord}".encode()).hexdigest()` and returns `f"REQ-{h[:10]}"`, importing normalize from grounding.normalize so the hash key uses the same canonical form as grounding (per key_links). display_label returns `f"{section_label}-{ordinal}"`. Document that display labels may renumber but requirement_id must not.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_ids.py -q` passes including the stability, sibling-distinctness, occurrence-distinctness, and normalize-equivalence cases.
    - `grep -q "from rfp_analyzer.pipeline.grounding.normalize import normalize" src/rfp_analyzer/pipeline/ids.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_ids.py -q</automated></verify>
  <done>requirement_id is stable and content-derived; normalize-equivalent verbatims collapse to one id; siblings and repeat occurrences stay distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document text → schema | Parsed RFP text (untrusted) is stored in these models; no execution, only data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | RequirementBatch used as Ollama `format` schema | mitigate | Keep schema flat (no constrained numerics / recursion) so grammar-constrained decoding cannot be bypassed by a malformed schema; asserted in Task 1 tests |
| T-02-02 | Repudiation | requirement_id collisions hide/merge distinct requirements | mitigate | sha256 over (file_id|normalized_verbatim|occurrence|atomic_ord); occurrence + atomic_ord guarantee distinct rows for repeated/atomic-split text (Task 3) |
</threat_model>

<verification>
- `uv run python -m pytest tests/unit/test_requirement_models.py tests/unit/test_normalize.py tests/unit/test_ids.py -q` all pass.
- `uv run ruff check src/rfp_analyzer/pipeline/models.py src/rfp_analyzer/pipeline/grounding src/rfp_analyzer/pipeline/ids.py` clean.
- All three tests run pure-Python in CI (no Ollama, no corpus, no GPU).
</verification>

<success_criteria>
The Phase 2 schema, normalizer, and stable-ID scheme exist as importable, tested
primitives; SourceRef carries doc_role provenance; RequirementBatch's JSON schema is
Ollama-structured-output-safe; ID assignment is provably deterministic and
content-derived.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
