---
phase: 02-requirement-extraction-grounding
plan: 07
type: execute
wave: 5
depends_on: ["02-04", "02-06"]
files_modified:
  - tests/eval/metrics.py
  - tests/eval/conftest.py
  - tests/eval/test_bakeoff.py
  - tests/eval/BAKEOFF.md
  - src/rfp_analyzer/cli.py
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03, EXTR-05]

must_haves:
  truths:
    - "The eval harness scores extraction output against the golden set with a matching rule (fuzzy verbatim overlap AND same file+page) and computes precision, recall, and F1 — reported SEPARATELY, not just F1 (success criterion 5)"
    - "A bake-off runs both qwen2.5:14b-instruct and qwen2.5:32b-instruct against the golden set and reports precision/recall/F1 plus latency and GPU/throughput cost per model, so the winner is chosen on evidence"
    - "recall and precision are surfaced on every extraction run (the CLI report prints them when a golden set is available), not only in the bake-off"
    - "The bake-off is documented in BAKEOFF.md with the recorded numbers and the model recommendation"
  artifacts:
    - path: "tests/eval/metrics.py"
      provides: "is_match() + score() precision/recall/F1"
      exports: ["is_match", "score"]
    - path: "tests/eval/test_bakeoff.py"
      provides: "14b vs 32b bake-off runner (skipif Ollama)"
    - path: "tests/eval/BAKEOFF.md"
      provides: "recorded bake-off results + model recommendation"
  key_links:
    - from: "tests/eval/metrics.py"
      to: "tests/eval/golden/golden_set.json"
      via: "loads ground truth and scores predictions against it"
      pattern: "golden_set.json"
    - from: "tests/eval/test_bakeoff.py"
      to: "src/rfp_analyzer/pipeline/extraction/run_extraction.py"
      via: "runs extraction per model then scores with metrics.score"
      pattern: "run_extraction"
---

<objective>
Close the phase with the objective signal (success criterion 5): a recall/precision/F1
eval harness scoring extraction against the adversarially-validated golden set, and
a 14B-vs-32B bake-off that picks the model on measured recall, precision, latency, and
GPU cost. Recall and precision are reported SEPARATELY every run.

Purpose: Extraction accuracy becomes measurable and the model choice becomes
evidence-driven, per the locked balanced-F1 decision.
Output: `tests/eval/metrics.py`, `test_bakeoff.py`, `BAKEOFF.md`; a golden-scored
recall/precision line in the CLI report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-extraction-grounding/02-CONTEXT.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/extraction/run_extraction.py
@tests/eval/golden/golden_set.json
@src/rfp_analyzer/cli.py

<interfaces>
<!-- From 02-06: run_extraction(document_map, model, seed) -> RequirementSet.
     From 02-04: golden_set.json (list of ground-truth entries: file_id, page, verbatim_text,
     atomic_obligation, binding_keyword, req_type).
     From 02-01: Requirement(source_ref{file_id,page}, verbatim_text, ...); RequirementSet.
     From 02-01: normalize import path grounding.normalize.
     RESEARCH matching rule: pred matches gold iff same file_id AND same page AND
     token_set_ratio(normalize(pred.verbatim), normalize(gold.verbatim)) >= MATCH_THRESHOLD (~90).
     precision = matched_preds/total_preds; recall = matched_golds/total_golds; f1 = harmonic mean. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Eval matching rule + precision/recall/F1 (metrics.py)</name>
  <files>tests/eval/metrics.py, tests/eval/conftest.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md ("Precision/recall matching rule" code example + Assumption A2 thresholds + Pitfall 6 determinism), src/rfp_analyzer/pipeline/grounding/normalize.py, tests/eval/golden/golden_set.json (entry shape)</read_first>
  <behavior>
    - is_match(pred, gold): True only when same file_id, same page, and normalized token_set_ratio ≥ MATCH_THRESHOLD; False when page differs even if text matches.
    - score(preds, golds) returns a dict with precision, recall, f1, matched, total_preds, total_golds — precision and recall are distinct fields (not collapsed into F1).
    - Each gold matched at most once (no double-counting inflating recall).
    - Empty preds → recall 0.0, precision defined (0.0 by convention) without ZeroDivisionError.
  </behavior>
  <action>Create tests/eval/metrics.py exporting `is_match(pred, gold, thresh: float = 90.0) -> bool` and `score(preds, golds, thresh: float = 90.0) -> dict`, implementing the RESEARCH matching rule with rapidfuzz `fuzz.token_set_ratio` over normalized verbatims plus a file_id+page equality gate. Greedily match each gold to at most one pred. Compute precision/recall/f1 separately (guard divide-by-zero). Add a golden-set loader in conftest.py (`load_golden()`), and expose MATCH_THRESHOLD as a tunable module constant (eval knob, A2). This is a pure-Python test-support module — CI-testable with tiny synthetic pred/gold fixtures, no Ollama.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/eval/metrics.py tests/eval -q -k "metric or match"` (or a dedicated test file) passes the same-page/diff-page, single-match, and empty-preds cases.
    - `grep -q "token_set_ratio" tests/eval/metrics.py` and `grep -q "def score" tests/eval/metrics.py`.
    - A test asserts score returns distinct precision and recall keys.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/eval -q -k "metric or match or score"</automated></verify>
  <done>The eval computes precision, recall, and F1 separately via a page-gated fuzzy-verbatim matching rule — the objective signal for success criterion 5.</done>
</task>

<task type="auto">
  <name>Task 2: 14b-vs-32b bake-off runner + BAKEOFF.md</name>
  <files>tests/eval/test_bakeoff.py, tests/eval/conftest.py, tests/eval/BAKEOFF.md</files>
  <read_first>tests/eval/metrics.py (Task 1), src/rfp_analyzer/pipeline/extraction/run_extraction.py, tests/integration/conftest.py (ollama_available skip pattern from 02-06), .planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pitfalls 6,7 — GPU nondeterminism tolerance, 32B VRAM spill; report to 2 sig figs; measure tok/s + GPU%)</read_first>
  <action>Create test_bakeoff.py that, when Ollama + the golden-set source corpus are available (else skipif — CI has neither), runs run_extraction over the primary-ucf DocumentMap for BOTH "qwen2.5:14b-instruct" and "qwen2.5:32b-instruct", scores each against load_golden() via metrics.score, and asserts each model exceeds a modest floor (e.g., recall ≥ a documented minimum) so a regression fails the eval. Capture per-model precision, recall, f1, wall-clock latency, and tokens (from RunMetrics); for the 32B run note GPU%/throughput if observable (Pitfall 7). Write BAKEOFF.md recording the actual numbers for both models, latency/GPU cost, the balanced-F1-based recommendation, and the honest determinism caveat (numbers to ~2 sig figs; re-run if a decision is close). Keep the pass/fail floor tolerant of small variance; the bake-off's job is comparison + regression floor, not brittle exact-count assertions.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/eval/test_bakeoff.py -q` runs both models locally and PASSES, and SKIPS cleanly in CI (no Ollama) — verify the skip.
    - BAKEOFF.md contains a table with precision/recall/F1 + latency for both 14b and 32b and a one-line recommendation.
    - `grep -q "run_extraction" tests/eval/test_bakeoff.py` and both model IDs appear in the file.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/eval/test_bakeoff.py -q</automated></verify>
  <done>A reproducible bake-off measures both Qwen sizes on recall/precision/F1 + latency/GPU cost against the golden set and records an evidence-based model recommendation.</done>
</task>

<task type="auto">
  <name>Task 3: Surface recall/precision on every extract run</name>
  <files>src/rfp_analyzer/cli.py, tests/eval/conftest.py</files>
  <read_first>src/rfp_analyzer/cli.py (the extract report from 02-06), tests/eval/metrics.py (Task 1), .planning/phases/02-requirement-extraction-grounding/02-CONTEXT.md (report recall AND precision separately, every run)</read_first>
  <action>Extend the `extract` subcommand's report so that WHEN the run targets the golden-set package (primary-ucf) and the golden set is present, the report prints a "vs golden set: precision=…, recall=…, F1=…" line computed via metrics.score — satisfying "reported on every run" (success criterion 5). When no golden set applies to the package, print "no golden set for this package (recall/precision not scored)" so the absence is explicit, never silently omitted. Keep the eval import lazy/optional so a normal extract of an arbitrary package does not require the golden fixture. Add a `--golden PATH` flag to point at an alternate golden file.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/eval -q -k "report or golden_line"` passes a test that, given a RequirementSet for the golden package plus the golden set, the report string contains both "precision=" and "recall=" (separately) and "F1=".
    - A test asserts a non-golden package prints the explicit "no golden set" line (absence surfaced, not hidden).
    - `grep -q "recall=" src/rfp_analyzer/cli.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/eval -q -k "report or golden_line"</automated></verify>
  <done>Recall and precision (separately) plus F1 appear on every golden-package extract run; non-golden packages get an explicit not-scored line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden set → eval score | The score is only as honest as the golden set (see 02-04 validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Repudiation | Reporting only F1 hides a recall or precision regression | mitigate | score() exposes precision and recall as distinct fields; CLI + bake-off print them separately every run (Tasks 1,3) |
| T-02-18 | Tampering | GPU nondeterminism makes a close bake-off decision unreproducible | accept | temperature 0 + fixed seed; report to 2 sig figs; re-run when close; rely on the 14b/32b delta exceeding noise (RESEARCH Pitfall 6) |
</threat_model>

<verification>
- `uv run python -m pytest tests/eval -q` passes the pure metrics/report tests in CI; the bake-off test skips without Ollama.
- `uv run ruff check tests/eval src/rfp_analyzer/cli.py` clean.
- Local: bake-off run produces BAKEOFF.md with real numbers for both models (evidence in SUMMARY).
</verification>

<success_criteria>
Extraction accuracy is measured as separately-reported recall/precision/F1 against
the adversarially-validated golden set on every run, and the 14B-vs-32B model choice
is made on recorded evidence (recall/precision/latency/GPU) — satisfying phase
success criterion 5.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-07-SUMMARY.md` when done.
</output>
