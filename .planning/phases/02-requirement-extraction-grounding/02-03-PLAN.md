---
phase: 02-requirement-extraction-grounding
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/rfp_analyzer/pipeline/extraction/__init__.py
  - src/rfp_analyzer/pipeline/extraction/chunker.py
  - src/rfp_analyzer/pipeline/sweep.py
  - src/rfp_analyzer/pipeline/amendments.py
  - tests/unit/test_chunker.py
  - tests/unit/test_sweep.py
  - tests/unit/test_amendments.py
autonomous: true
requirements: [EXTR-03, EXTR-04, EXTR-05, INTK-03]

must_haves:
  truths:
    - "The chunker emits chunks for EVERY file and section in the map — including attachments and Sections C/H — not just L/M, so requirements outside L/M are reachable (EXTR-04)"
    - "Every chunk carries a page_map linking its char ranges to real page numbers, and skips non-ok (scanned/gibberish/empty) pages"
    - "A deterministic shall/must/will/should sweep over cleaned page text finds binding-keyword sentences independently of the model, and reconciliation surfaces sweep hits no extracted requirement covers as missed_candidates (EXTR-05)"
    - "SF30 amendment change statements are flagged is_amendment_change; base requirements they reference are marked possibly_modified with an affects pointer — nothing is merged (INTK-03)"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/extraction/chunker.py"
      provides: "iter_chunks(document_map) -> Iterator[Chunk] over all files/sections with page_map"
      exports: ["iter_chunks"]
    - path: "src/rfp_analyzer/pipeline/sweep.py"
      provides: "sweep_hits() + reconcile() (missed candidates + authoritative binding keyword)"
      exports: ["sweep_hits", "reconcile"]
    - path: "src/rfp_analyzer/pipeline/amendments.py"
      provides: "flag_amendments() SF30 change detection + possibly_modified flagging"
      exports: ["flag_amendments"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/extraction/chunker.py"
      to: "DocumentMap.files[].sections + pages"
      via: "walks every section locator, concatenates ok-page text, builds page_map"
      pattern: "page_map"
    - from: "src/rfp_analyzer/pipeline/sweep.py"
      to: "src/rfp_analyzer/pipeline/grounding/normalize.py"
      via: "normalizes page text and requirement spans before overlap comparison"
      pattern: "from rfp_analyzer.pipeline.grounding.normalize import normalize"
---

<objective>
Build the three pure-Python transformations that surround the model call:
(1) the section-scoped **chunker** that feeds the model and preserves page
provenance for grounding and reaches attachments/C/H (EXTR-04); (2) the
deterministic **keyword sweep + reconciliation** that is the independent
cross-check and the authoritative binding-keyword source (EXTR-03/05); (3) the
SF30 **amendment** change detector that flags without merging (INTK-03).

Purpose: The model-free correctness machinery — all CI-testable, no GPU.
Output: `extraction/chunker.py`, `sweep.py`, `amendments.py`; unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/grounding/normalize.py

<interfaces>
<!-- From Phase 1 models.py: -->
ParsedFile(file_id, filename, parse_status, doc_role, pages:list[PageInfo], sections:list[SectionNode])
PageInfo(page_number:int, quality:Literal["ok","scanned","empty","low_text","gibberish"], text:str)
SectionNode(label, title, role, locator, children:list[SectionNode])   # recursive
PageSpan(page_start:int, page_end:int)  # locator.kind=="pages"; DOCX uses BlockSpan (no pages)
<!-- From 02-01: -->
Chunk(file_id, filename, section_label, role, doc_role, text, page_map:list[tuple[int,int,int]])
Requirement(requirement_id, verbatim_text, binding_keyword, source_ref, possibly_modified,
  affects, is_amendment_change, ...); MissedCandidate(file_id, page, verbatim_sentence, binding_keyword)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Section-scoped chunker with page_map (EXTR-04 reach)</name>
  <files>src/rfp_analyzer/pipeline/extraction/__init__.py, src/rfp_analyzer/pipeline/extraction/chunker.py, tests/unit/test_chunker.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 1 + Pitfalls 3,4), src/rfp_analyzer/pipeline/models.py, .planning/phases/01-parsing-structure-foundation/01-06-SUMMARY.md (primary package shape: 161-page Section C Annexes, SECTION F ANNEX — the windowing targets)</read_first>
  <behavior>
    - iter_chunks over a map with L, M, an attachment SOW, and a Section C emits ≥1 chunk for EACH (proves EXTR-04 attachments/C are covered, not just L/M).
    - A section spanning pages 49–57 (all quality ok) yields one Chunk whose page_map covers 49..57 and whose text concatenates those pages' text in order.
    - Non-ok pages (scanned/gibberish/empty) are skipped and excluded from page_map.
    - A section larger than max_input_chars is split into multiple overlapping chunks (≈1 page overlap); each chunk's page_map is correct for its own text.
    - DOCX files (BlockSpan locators, no pages) are handled without synthesizing page numbers — either skipped for page-grounded chunking or emitted with page_map pages set from block ordinals? Use: skip DOCX page-grounding for this phase and emit no page_map entries (documented) — never invent a PDF page for DOCX.
  </behavior>
  <action>Create the `extraction` package (`__init__.py` docstring) and `chunker.py` exporting `iter_chunks(document_map, max_input_chars: int = 48000) -> Iterator[Chunk]`. Iterate ALL files with parse_status=="ok" and recursively walk every section (RESEARCH `_walk_sections`); for each PDF section, gather the PageInfo objects whose page_number falls in the PageSpan and whose quality=="ok", concatenate their text, and build page_map as a list of (char_start, char_end, page_number) tuples in text order. Window sections whose text exceeds max_input_chars into overlapping page windows (~1 page overlap) so no chunk exceeds the budget (Pitfall 3/4 — prefer smaller chunks). Set section_label from the SectionNode.label (fallback to role or filename for attachments), role from SectionNode.role, doc_role from ParsedFile.doc_role. max_input_chars is a tunable default (RESEARCH A1: ~600–1000 tok/page; leave char budget conservative and eval-tunable). Do not synthesize pages for DOCX BlockSpan sections.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_chunker.py -q` passes, including the "attachment + C section each produce a chunk" test (EXTR-04) and the page_map-correctness test.
    - `grep -q "def iter_chunks" src/rfp_analyzer/pipeline/extraction/chunker.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_chunker.py -q</automated></verify>
  <done>iter_chunks covers every file/section (incl. attachments & C/H), skips non-ok pages, preserves page provenance, and windows oversize sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic keyword sweep + reconciliation (EXTR-05)</name>
  <files>src/rfp_analyzer/pipeline/sweep.py, tests/unit/test_sweep.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 6 + Open Question 4 on sentence segmentation), src/rfp_analyzer/pipeline/grounding/normalize.py</read_first>
  <behavior>
    - sweep_hits finds every sentence containing shall/shall not/must/will/should/is required to/are required to on a page, returning (page, verbatim_sentence, keyword); "shall not" wins over "shall" when both are present.
    - Abbreviation-guarded segmentation does NOT split on "No.", "U.S.", "e.g.", or "L.4.2" (section numbers).
    - reconcile(requirements, hits): a hit is "covered" when some requirement's normalized verbatim overlaps the hit sentence on the same page; uncovered hits become MissedCandidate rows.
    - reconcile also sets each requirement's authoritative binding_keyword from the sweep hit that covers it (source-derived keyword beats the model's guess, per Pattern 6).
    - A requirement whose page has no matching sweep keyword is left flaggable (return it in a separate "possible over-extraction" list or mark it) for the precision side.
  </behavior>
  <action>Create sweep.py exporting `sweep_hits(page_text: str, page_number: int) -> list[SweepHit]` and `reconcile(requirements: list[Requirement], hits: list[SweepHit]) -> tuple[list[Requirement], list[MissedCandidate]]`. BINDING regex per RESEARCH: `re.compile(r"\b(shall not|shall|must|will|should|is required to|are required to)\b", re.I)`. Implement a small abbreviation-guarded sentence splitter in pure Python (no new dep). reconcile compares normalized spans on the same page number; uncovered hits → MissedCandidate; covered requirements get binding_keyword set from the hit. Define a small internal SweepHit dataclass/model. This module is the independent cross-check — it over-generates by design (it is a recall floor, not ground truth); document that.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_sweep.py -q` passes, including the abbreviation-guard cases and a reconcile test where one sweep hit is uncovered → exactly one MissedCandidate.
    - `grep -q "def reconcile" src/rfp_analyzer/pipeline/sweep.py` and `grep -q "shall not" src/rfp_analyzer/pipeline/sweep.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_sweep.py -q</automated></verify>
  <done>The sweep independently finds binding sentences, reconciliation surfaces model misses as missed_candidates, and supplies the authoritative binding keyword.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SF30 amendment change detection (INTK-03)</name>
  <files>src/rfp_analyzer/pipeline/amendments.py, tests/unit/test_amendments.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 7 + Assumption A7), src/rfp_analyzer/pipeline/classify/forms.py (doc_role="amendment" contract; amendment_number often None), .planning/phases/01-parsing-structure-foundation/01-06-SUMMARY.md (SF30 Block 2 AcroForm reality)</read_first>
  <behavior>
    - A requirement whose verbatim contains change language ("is hereby changed", "is revised", "delete … and insert", "in lieu of", "is changed to read") is flagged is_amendment_change=True.
    - A change statement referencing a base section (e.g. "Section L.4.2 is changed to read…") adds an affects pointer to base requirements in that section and sets their possibly_modified=True — BOTH rows are retained (no merge).
    - Requirements from base_solicitation files are never auto-flagged as changes.
    - Runs without an amendment_number (None is an honest outcome) and still produces valid change rows.
  </behavior>
  <action>Create amendments.py exporting `flag_amendments(requirements: list[Requirement]) -> list[Requirement]`. Use a CHANGE regex per RESEARCH Pattern 7: `is (hereby )?(changed|revised|amended|deleted|added|replaced)`, `delete .* and (insert|substitute)`, `in lieu of`, `is changed to read`. Only consider requirements whose source_ref/doc_role is "amendment" as candidate change statements. For each change statement, set is_amendment_change=True; if it names a base section label, find base requirements whose source_ref.section_label matches and set their possibly_modified=True and append the change's requirement_id to their affects list. Never delete or merge rows. amendment_number recovery via AcroForm is explicitly OUT of scope here (RESEARCH Open Question 3 — nice-to-have; None stays valid).</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_amendments.py -q` passes, including: a change statement flagged; a referenced base row marked possibly_modified with the change id in affects; both rows still present (count unchanged); a base-only set produces zero change flags.
    - `grep -q "is_amendment_change" src/rfp_analyzer/pipeline/amendments.py` and `grep -q "possibly_modified" src/rfp_analyzer/pipeline/amendments.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_amendments.py -q</automated></verify>
  <done>SF30 change statements become their own flagged rows, referenced base requirements are marked possibly_modified, and nothing is merged — satisfying INTK-03's no-silent-merge rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document text → sweep/chunker | Untrusted RFP text is scanned by pure-Python regex only; never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Amendment change silently overwrites a base requirement | mitigate | flag_amendments only flags (possibly_modified + affects), never merges/deletes — asserted by row-count-unchanged test (Task 3) |
| T-02-06 | Denial of Service | Pathological huge section blows the model context | mitigate | chunker caps chunk size and windows oversize sections (Task 1) — bounds every downstream model call |
| T-02-07 | Info Disclosure | Model misses a binding requirement, corrupting the matrix | mitigate | Independent deterministic sweep + reconciliation surfaces misses as missed_candidates (Task 2) |
</threat_model>

<verification>
- `uv run python -m pytest tests/unit/test_chunker.py tests/unit/test_sweep.py tests/unit/test_amendments.py -q` all pass — pure-Python, CI-safe (no Ollama/GPU/corpus).
- `uv run ruff check src/rfp_analyzer/pipeline/extraction/chunker.py src/rfp_analyzer/pipeline/sweep.py src/rfp_analyzer/pipeline/amendments.py` clean.
</verification>

<success_criteria>
Chunking reaches every file/section with page provenance (EXTR-04); the deterministic
sweep independently surfaces model misses and supplies binding keywords (EXTR-03/05);
SF30 changes are flagged without merging (INTK-03) — all provable in CI without a GPU.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-03-SUMMARY.md` when done.
</output>
