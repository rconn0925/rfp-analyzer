---
phase: 02-requirement-extraction-grounding
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/rfp_analyzer/pipeline/extraction/prompt.py
  - src/rfp_analyzer/pipeline/extraction/client.py
  - src/rfp_analyzer/pipeline/extraction/extract.py
  - tests/unit/test_client.py
  - tests/unit/test_extract.py
autonomous: true
requirements: [EXTR-01, EXTR-02, EXTR-03]

must_haves:
  truths:
    - "Every Ollama call passes options with num_ctx=32768, temperature=0, a fixed seed, and num_predict=-1 — the num_ctx line is enforced so a long chunk is never silently truncated"
    - "The client uses the native ollama Client (not the OpenAI-compat /v1 endpoint) with format=RequirementBatch.model_json_schema() so output is guaranteed-valid JSON"
    - "extract turns each chunk into grounded Requirements: atomic split preserved via parent_index, every verbatim grounded through build_source_ref (which threads chunk.doc_role onto the SourceRef), stable ids assigned with a deterministic ordering even for ungroundable rows, req_type set from SectionNode.role (ALL role values mapped) reconciled with the model guess"
    - "A per-chunk parse failure is isolated (recorded, run continues) — one bad chunk never crashes extraction"
  artifacts:
    - path: "src/rfp_analyzer/pipeline/extraction/client.py"
      provides: "extract_chunk() — the only network-touching module; num_ctx guard"
      exports: ["extract_chunk"]
    - path: "src/rfp_analyzer/pipeline/extraction/extract.py"
      provides: "extract_requirements(chunks, model, seed) -> list[Requirement]"
      exports: ["extract_requirements"]
    - path: "src/rfp_analyzer/pipeline/extraction/prompt.py"
      provides: "SYSTEM_PROMPT enforcing verbatim copy + atomic split"
      exports: ["SYSTEM_PROMPT"]
  key_links:
    - from: "src/rfp_analyzer/pipeline/extraction/client.py"
      to: "ollama.Client(host='http://localhost:11434')"
      via: "chat(format=schema, options={num_ctx:32768,...})"
      pattern: "num_ctx"
    - from: "src/rfp_analyzer/pipeline/extraction/extract.py"
      to: "src/rfp_analyzer/pipeline/grounding/verify.py"
      via: "build_source_ref grounds every draft verbatim and threads chunk.doc_role"
      pattern: "build_source_ref"
---

<objective>
Build the model-calling layer (the only network-touching code in the pure
library) and the orchestration that turns chunks into grounded, atomic,
stably-identified, typed Requirements (EXTR-01/02/03). The num_ctx guard is the
#1 recall protection; grounding every draft is the honesty gate.

Purpose: Extract requirements with guaranteed-valid JSON, verbatim fidelity, and
computed-not-claimed provenance.
Output: `extraction/prompt.py`, `client.py`, `extract.py`; `ollama` dependency;
unit tests (model-calling ones skipif Ollama unreachable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md
@src/rfp_analyzer/pipeline/models.py
@src/rfp_analyzer/pipeline/metrics.py
@src/rfp_analyzer/pipeline/grounding/verify.py
@src/rfp_analyzer/pipeline/extraction/chunker.py

<interfaces>
<!-- From 02-01: RequirementBatch, RequirementDraft, Requirement, Chunk, SourceRef(...,doc_role).
     From 02-02: build_source_ref(verbatim, chunk, threshold) -> SourceRef (SourceRef.doc_role = chunk.doc_role, both paths).
     From 02-03: Chunk(...page_map, doc_role...) from iter_chunks.
     From 02-01 ids.py: requirement_id(file_id, verbatim, occurrence, atomic_ord), display_label.
     SectionNode.role enum (models.py): instructions | evaluation | sow_pws |
       special_requirements | clauses | attachments_list | other | None. -->
<!-- ollama 0.6.2 (verified in RESEARCH): -->
from ollama import Client
Client(host="http://localhost:11434").chat(
  model, messages=[{role,content}...], format=<json schema>,
  options={"num_ctx":32768,"temperature":0,"seed":N,"num_predict":-1},
  keep_alive="30m", stream=False)  -> resp with resp.message.content (JSON str),
                                       resp.prompt_eval_count, resp.eval_count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extraction prompt + Ollama client with num_ctx guard</name>
  <files>pyproject.toml, uv.lock, src/rfp_analyzer/pipeline/extraction/prompt.py, src/rfp_analyzer/pipeline/extraction/client.py, tests/unit/test_client.py</files>
  <read_first>.planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Patterns 2 + Pitfalls 1,2,3,6,7; "Extraction call" code example; Package Legitimacy Audit — ollama is [OK]/authoritative, no legitimacy checkpoint needed), CLAUDE.md (pure-library boundary: localhost HTTP allowed, no web-framework deps)</read_first>
  <action>Run `uv add ollama` (authoritative package per RESEARCH audit — no legitimacy gate required). Create prompt.py exporting `SYSTEM_PROMPT` that instructs the model to (1) copy verbatim_text EXACTLY from the source with zero paraphrase/summary/reformatting (Pitfall 2), (2) put any rewording only in atomic_obligation, (3) split compound obligations into atomic rows linked by parent_index per the Pattern 3 rule, (4) classify type_guess and binding_keyword. Optionally include 1-2 few-shot examples. Create client.py exporting `extract_chunk(chunk_text: str, model: str, seed: int = 7) -> RequirementBatch` using the native `ollama.Client(host="http://localhost:11434")` with `format=RequirementBatch.model_json_schema()`, `options={"num_ctx":32768,"temperature":0,"seed":seed,"num_predict":-1}`, `keep_alive="30m"`, `stream=False`, returning `RequirementBatch.model_validate_json(resp.message.content)`. Add a pure `estimate_tokens(text)` heuristic and a `_assert_fits(chunk_text, num_ctx=32768, reserved_output=12000)` guard that raises if the input would exceed the budget (Pitfall 1). Capture resp.prompt_eval_count/eval_count for the caller to fold into RunMetrics. Wrap model_validate_json so a truncated/invalid JSON raises a typed ExtractionParseError the orchestrator can isolate (Pitfall 3). Do NOT use the OpenAI-compat /v1 endpoint.</action>
  <acceptance_criteria>
    - `grep -q "num_ctx" src/rfp_analyzer/pipeline/extraction/client.py` and `grep -q "32768" src/rfp_analyzer/pipeline/extraction/client.py`.
    - Source assertion: client.py imports from `ollama` and does NOT reference `/v1` or `openai`.
    - `grep -q "ollama" pyproject.toml`.
    - Pure guard test passes: `uv run python -m pytest tests/unit/test_client.py -q` — includes an `_assert_fits` over-budget case that raises, and a SYSTEM_PROMPT contains-"verbatim" assertion. Model-calling tests (a live extract_chunk) are marked `skipif` Ollama unreachable (mirror the corpus-skip pattern) and are NOT required to run in CI.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_client.py -q</automated></verify>
  <done>The only network module wraps native Ollama with the enforced num_ctx guard, grammar-constrained JSON, a verbatim-fidelity system prompt, and isolated parse errors.</done>
</task>

<task type="auto">
  <name>Task 2: extract_requirements orchestration (chunk → grounded Requirement)</name>
  <files>src/rfp_analyzer/pipeline/extraction/extract.py, tests/unit/test_extract.py</files>
  <read_first>src/rfp_analyzer/pipeline/extraction/client.py (Task 1), src/rfp_analyzer/pipeline/grounding/verify.py, src/rfp_analyzer/pipeline/ids.py, src/rfp_analyzer/pipeline/models.py (SectionNode.role FULL enum: instructions/evaluation/sow_pws/special_requirements/clauses/attachments_list/other), .planning/phases/02-requirement-extraction-grounding/02-RESEARCH.md (Pattern 3 atomic assembly, Pattern 4 occurrence/atomic_ord, Pattern 5 grounding, EXTR-03 type reconciliation)</read_first>
  <action>Create extract.py exporting `extract_requirements(chunks: Iterable[Chunk], model: str, seed: int = 7, extract_fn=extract_chunk) -> list[Requirement]`. For each chunk: call extract_fn to get a RequirementBatch (isolate ExtractionParseError per-chunk — record and continue, Pitfall 3); for each RequirementDraft build a Requirement: ground verbatim_text via build_source_ref(draft.verbatim_text, chunk) → SourceRef and verified flag (the SourceRef already carries chunk.doc_role from 02-02, on both grounded and ungroundable paths — do not re-copy it, and do NOT drop it: it must survive onto the Requirement's source_ref so amendment gating in 02-03 works). Compute `occurrence` and `atomic_ord`, then assign requirement_id(chunk.file_id, verbatim, occurrence, atomic_ord). OCCURRENCE ORDERING (both cases must be deterministic and stable across re-runs): (a) for GROUNDED drafts (verified=True), occurrence is the rank of the grounded span by (page, char_start) order within the chunk; (b) for UNGROUNDABLE drafts (verified=False, page=None/char_start=None — no grounded span to order by), fall back to a deterministic ordinal = the draft's index within its chunk's RequirementBatch (chunks themselves are produced in deterministic iter_chunks order), so unverified rows still get stable, re-run-identical ids. Never let two rows in one chunk collide on (occurrence, atomic_ord). Set atomic_ord from parent_index sibling ordering; set parent_id by resolving parent_index to the sibling's id; set display_label. Reconcile req_type from chunk.role (SectionNode.role → EXTR-03 req_type) preferring the section role over the model's type_guess, falling back to type_guess when role is None. The role→req_type map MUST cover EVERY SectionNode.role value: instructions→"instruction", evaluation→"evaluation", sow_pws→"sow_pws", special_requirements→"special_requirements", clauses→"clause", attachments_list→"attachment", other→"other" — no role value silently falls through to type_guess (only role=None does). Carry binding_keyword from the draft (the sweep will later override with the authoritative source keyword in run_extraction). De-duplicate by requirement_id (overlap-window dupes collapse). Inject extract_fn as a parameter so tests can pass a fake batch WITHOUT Ollama — the whole assembly path is then CI-testable.</action>
  <acceptance_criteria>
    - `uv run python -m pytest tests/unit/test_extract.py -q` passes using a FAKE extract_fn (no Ollama): asserts (a) a compound draft with three atomic siblings yields three Requirements sharing one verbatim/SourceRef with distinct ids and correct parent_id linkage; (b) an ungroundable draft yields a Requirement with verified=False (not dropped) AND a stable requirement_id — running extract_requirements twice over the same chunks yields identical ids for the ungroundable rows (proves the draft-index fallback ordering); (c) req_type comes from the chunk role when present — with an explicit case for special_requirements→"special_requirements" and attachments_list→"attachment" (the two roles the old map omitted); (d) a chunk whose extract_fn raises ExtractionParseError does not crash the run and yields the other chunks' requirements; (e) an ungroundable draft from an amendment chunk yields a Requirement whose source_ref.doc_role=="amendment".
    - `grep -q "def extract_requirements" src/rfp_analyzer/pipeline/extraction/extract.py` and `grep -q "build_source_ref" src/rfp_analyzer/pipeline/extraction/extract.py`.
    - `grep -q "special_requirements" src/rfp_analyzer/pipeline/extraction/extract.py` and `grep -q "attachments_list" src/rfp_analyzer/pipeline/extraction/extract.py`.
  </acceptance_criteria>
  <verify><automated>uv run python -m pytest tests/unit/test_extract.py -q</automated></verify>
  <done>extract_requirements assembles grounded, atomic, stably-identified (incl. deterministic ids for ungroundable rows), fully-role-mapped, typed Requirements from chunks; doc_role rides through on source_ref; parse failures are isolated; the assembly is fully unit-tested without a GPU via an injected fake model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document text → LLM | RFP text (untrusted) enters the model in the user turn; extraction instructions stay in the system turn |
| LLM output → pipeline | Model JSON (untrusted) is Pydantic-validated and every verbatim is grounded before it becomes a Requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Info Disclosure | num_ctx silent truncation drops tail requirements (recall collapse) | mitigate | Enforced options.num_ctx=32768 + `_assert_fits` guard on native client (Task 1); Pitfall 1 |
| T-02-11 | Tampering | Prompt injection from document text ("ignore instructions…") | mitigate | Instructions in system turn only; document strictly in user turn; grounding gate (02-02) rejects any non-locatable "requirement"; extracted text never executed |
| T-02-12 | Spoofing | Model paraphrases verbatim → false grounding to wrong page | mitigate | System prompt demands exact copy + temperature 0; grounding turns drift into a visible verified=False metric (Task 2) |
| T-02-13 | Info Disclosure | Ollama bound to 0.0.0.0 exposes local runtime | mitigate | Client targets http://localhost:11434 only; no remote bind in this phase |
| T-02-SC | Tampering | `ollama` install (pip supply chain) | accept | Authoritative package (official ollama org, RESEARCH audit [OK], slopcheck clean) — no legitimacy checkpoint required |
</threat_model>

<verification>
- `uv run python -m pytest tests/unit/test_client.py tests/unit/test_extract.py -q` pass in CI (fake model injection; live Ollama tests skipif-guarded).
- `uv run ruff check src/rfp_analyzer/pipeline/extraction` clean.
- Manual local smoke (developer machine, not CI): `extract_chunk` against qwen2.5:14b-instruct returns a valid RequirementBatch — recorded in the SUMMARY, not a CI gate.
</verification>

<success_criteria>
Chunks become grounded, atomic, stably-identified (deterministic ids even for
ungroundable rows), fully role-mapped Requirements (EXTR-01/02/03) via a native-Ollama
call that can never silently truncate; the full assembly is CI-testable without a GPU
through an injected fake model.
</success_criteria>

<output>
Create `.planning/phases/02-requirement-extraction-grounding/02-05-SUMMARY.md` when done.
</output>
</content>
</invoke>
